---
phase: 02-scip-swift-toolchain-update
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - tests/fixtures/mini_xcode_repo/scip-swift-test.xcodeproj/project.pbxproj
  - tests/fixtures/mini_xcode_repo/scip-swift-test.xcodeproj/project.xcworkspace/contents.xcworkspacedata
  - tests/fixtures/mini_xcode_repo/scip-swift-test/SwiftFile.swift
  - .github/workflows/setup-smoke.yml
autonomous: true
requirements: [SWFT-01, SWFT-03]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A git-tracked .xcodeproj fixture exists under tests/fixtures/mini_xcode_repo/ that a real jarvis index run publishes as language swift with status indexed — exercising _prefers_xcodebuild → --build-tool xcodebuild dispatch, the exact v0.2.x regression (D-10)"
    - "The setup-smoke workflow's macOS leg runs setup.sh and then a real jarvis index against the fixture, asserting the index publishes and no cache/build artifacts land inside the indexed tree (D-09)"
    - "Non-macOS CI legs skip the Swift index smoke without failing (the existing Linux skip-by-design contract is preserved)"
    - "The smoke step isolates itself from the runner's home: JARVIS_DATA_DIR and the fixture's git-init'd copy both live under RUNNER_TEMP, and ci-bin is prepended to PATH"
  artifacts:
    - path: "tests/fixtures/mini_xcode_repo/scip-swift-test.xcodeproj/project.pbxproj"
      provides: "minimal single-target tool project (no signing, one auto-resolvable scheme) proven indexable through jarvis"
    - path: ".github/workflows/setup-smoke.yml"
      provides: "post-install jarvis index smoke on the macOS leg + refreshed trigger paths and comments"
      contains: "mini_xcode_repo"
  key_links:
    - from: ".github/workflows/setup-smoke.yml (macOS leg)"
      to: "tests/fixtures/mini_xcode_repo/"
      via: "git-init'd RUNNER_TEMP copy indexed with ci-bin on PATH and JARVIS_DATA_DIR under RUNNER_TEMP — _prefers_xcodebuild fires on the .xcodeproj, scip-swift dispatches xcodebuild (the v0.3.0 fix), and the 02-01 cache-dir contract keeps the tree clean"
      pattern: "jarvis index"
  prohibitions:
    - "The CI smoke must not become a committed local integration test — no new @pytest.mark.integration test drives the fixture (user's explicit choice, D-09); the fixture is data for CI and manual verification only"
    - "The fixture must not be a generated-from-scratch pbxproj — it is adapted from upstream's committed Fixtures/XcodeTestProject at tag v0.3.0, proven buildable and indexable through jarvis's own argv (RESEARCH Don't-Hand-Roll)"

---

<objective>
Expansion plan 2 of 2: prove _swift_indexer_cmd compatibility against the resolved release the way the user chose — CI smoke only (D-09) on a new minimal .xcodeproj fixture (D-10) — and assert the out-of-tree-cache contract inside that same CI step.

Purpose: the v0.2.x regression was precisely xcodebuild dispatch being silently ignored; mini_swift_repo (Package.swift-only) cannot exercise it. A macOS runner with real Xcode indexing a committed .xcodeproj fixture is the only honest proof that v0.3.0's restored dispatch works through jarvis's argv, and it doubles as the standing guard against future upstream regressions.

Output: tests/fixtures/mini_xcode_repo/ (adapted from upstream XcodeTestProject at v0.3.0) and the setup-smoke.yml post-install index step with tree-cleanliness assertions.
</objective>

<execution_context>
@/Users/ddphuong/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scip-swift-toolchain-update/02-CONTEXT.md
@.planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md
@.planning/phases/02-scip-swift-toolchain-update/02-PATTERNS.md
@.planning/phases/02-scip-swift-toolchain-update/02-01-SUMMARY.md
@.github/workflows/setup-smoke.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the mini_xcode_repo fixture adapted from upstream XcodeTestProject</name>
  <precondition>Read access to jarvis-intelligence/scip-swift at tag v0.3.0 (authenticated gh CLI or git clone over https — both verified working on this host), and local xcodebuild available to validate the fixture indexes (Xcode 26.3 present)</precondition>
  <files>tests/fixtures/mini_xcode_repo/scip-swift-test.xcodeproj/project.pbxproj, tests/fixtures/mini_xcode_repo/scip-swift-test.xcodeproj/project.xcworkspace/contents.xcworkspacedata, tests/fixtures/mini_xcode_repo/scip-swift-test/SwiftFile.swift</files>
  <read_first>
  - .planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md — Pattern 4 (fixture anatomy: single com.apple.product-type.tool target, no signing/provisioning — the same rationale as setup.sh's v0.1.2 comment; objectVersion 77 / compatibilityVersion "Xcode 14.0"; measured cold 13-26s / warm 3.7s builds) and the CI note (git-tracked requirement, RUNNER_TEMP/ci-bin PATH, JARVIS_DATA_DIR isolation)
  - .planning/phases/02-scip-swift-toolchain-update/02-PATTERNS.md — fixture analogs (tests/fixtures/mini_swift_repo structure conventions; upstream Fixtures/XcodeTestProject contents and byte sizes)
  - tests/fixtures/mini_swift_repo/ (sibling fixture layout: what git-tracking looks like for fixtures in this repo)
  - src/jarvis/index_cli.py:193-214 (_prefers_xcodebuild — what the fixture must trigger: a checked-in .xcodeproj directory)
  </read_first>
  <action>
  Fetch upstream at the pinned tag (gh repo clone jarvis-intelligence/scip-swift with --depth 1 --branch v0.3.0, or git clone + checkout). Copy Fixtures/XcodeTestProject's three essential files into tests/fixtures/mini_xcode_repo/ preserving inner names — scip-swift-test.xcodeproj/project.pbxproj, scip-swift-test.xcodeproj/project.xcworkspace/contents.xcworkspacedata, scip-swift-test/SwiftFile.swift — deliberately keeping the upstream project name rather than renaming: pbxproj internals reference it throughout, and the fixture's identity is its directory (mini_xcode_repo, matching the mini_swift_repo/mini_py_repo convention). Do not hand-edit the pbxproj. git add the fixture so language detection's git ls-files sees it.

  Validate locally before committing (this is verification, not a committed test — D-09): copy the staged fixture to a fresh tmp dir, git init + git add + git commit there, then with JARVIS_DATA_DIR pointed at a second tmp dir and the Task-1 02-01 binary layout on PATH, run jarvis index on the copy. Require: exit 0, reported language swift, status indexed (indexed, not partial — navigation data present, proving xcodebuild genuinely dispatched), and the cache under <tmp-data>/cache/scip-swift/<slug>/. Clean up temp dirs. Commit the fixture files with chore(02-03) (data, not behavior).
  </action>
  <verify>
    <automated>git ls-files tests/fixtures/mini_xcode_repo | wc -l returns 3 and test -f tests/fixtures/mini_xcode_repo/scip-swift-test.xcodeproj/project.pbxproj && uv run python -m pytest -m "not integration" -q</automated>
  </verify>
  <done>Three fixture files committed and git-tracked; local validation indexed the copy as swift/indexed via xcodebuild dispatch; the unit suite stays green</done>
  <acceptance_criteria>
  - git ls-files lists exactly the three fixture paths (pbxproj, xcworkspacedata, SwiftFile.swift) — git-tracked so detection and zoekt see them
  - The fixture contains no Package.swift (which would flip _prefers_xcodebuild off) and no signing/provisioning settings
  - pbxproj is byte-adapted from upstream v0.3.0, not authored fresh (no generator artifacts)
  - SUMMARY records the local validation evidence: language swift, status indexed, cache location
  - No new @pytest.mark.integration test was added anywhere (D-09 prohibition)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: setup-smoke.yml post-install jarvis index step on the macOS leg</name>
  <files>.github/workflows/setup-smoke.yml</files>
  <read_first>
  - .github/workflows/setup-smoke.yml (whole file): the scip-swift install step at 63-69 whose comment block at 58-62 still references SCIP_SWIFT_VERSION (stale after 02-01 — this task refreshes it), the astral-sh/setup-uv step at 77, and the unit-test step at 79-91 the new step follows
  - .planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md — Validation Architecture (phase gate: setup-smoke incl. the new macOS fixture-index step) and assumption A1 (macos-latest arm64 + Xcode — the flagged environment assumption this step exercises for real)
  - .planning/phases/02-scip-swift-toolchain-update/02-PATTERNS.md — workflow analog (the existing macOS-gated verify pattern with RUNNER_TEMP/ci-bin)
  </read_first>
  <action>
  In .github/workflows/setup-smoke.yml:

  1) Update the stale comment above the scip-swift install step (lines 58-62): it still names the old exact-tag pin variable, which 02-01 removed — reword to say the step is the only guard that the latest-resolution + floor + digest path in install_scip_swift actually resolves against the real API (Linux still exercises the skip branch).

  2) Add a new step after "Run the setup.sh unit tests" (so setup-uv is already available), named to state its purpose (post-install Swift index through jarvis), gated on RUNNER_OS = macOS inside the run block (matching the existing if-inside-run style at lines 67-69 rather than a job-level if, so the Linux leg still runs the step and proves the skip). The step:
  - Copies tests/fixtures/mini_xcode_repo to "${RUNNER_TEMP}/mini_xcode_repo" and git init + git add + git commit there (a standalone repo: language detection reads git ls-files and zoekt indexes git — indexing the in-checkout fixture dir directly would read the jarvis repo's git metadata; research's proven flow used a git-init'd copy)
  - Exports PATH="${RUNNER_TEMP}/ci-bin:$PATH" (scip, zoekt-git-index, scip-swift from the earlier install steps) and JARVIS_DATA_DIR="${RUNNER_TEMP}/jarvis-data"
  - Runs uv run jarvis index "${RUNNER_TEMP}/mini_xcode_repo" --slug mini-xcode-repo
  - Asserts success: the command's exit code (set -eu), then explicit tree-cleanliness checks — no .scip-cache and no .build inside "${RUNNER_TEMP}/mini_xcode_repo" (shell test-existence checks; the one-shot xcshareddata file is accepted and not asserted against, per orchestrator resolution #2), and the cache root exists at "${RUNNER_TEMP}/jarvis-data/cache/scip-swift/mini-xcode-repo/"
  - Echoes scip-swift --version before indexing so the resolved version is visible in logs (a future v0.4.0 still passes the floor but changes expectations — research's validity note)

  3) Extend both the push and pull_request path filters with "tests/fixtures/mini_xcode_repo/**" and "src/jarvis/**" so changes to the fixture or the indexer plumbing actually trigger the smoke.

  Commit chore(02-03). Then verify locally by structural inspection (the real execution proof is the next CI run; a workflow_dispatch push triggers it immediately).
  </action>
  <verify>
    <automated>sh -c "grep -c 'mini_xcode_repo' .github/workflows/setup-smoke.yml && grep -c 'JARVIS_DATA_DIR' .github/workflows/setup-smoke.yml && ! grep -q 'SCIP_SWIFT_VERSION' .github/workflows/setup-smoke.yml"</automated>
  </verify>
  <done>The macOS smoke leg indexes the fixture through a real jarvis run with tree-cleanliness assertions; Linux legs skip the index but still run the step; trigger paths cover the fixture and src/jarvis</done>
  <acceptance_criteria>
  - The workflow contains the new step referencing tests/fixtures/mini_xcode_repo, uv run jarvis index, RUNNER_TEMP/ci-bin PATH prepend, and JARVIS_DATA_DIR isolation
  - The tree-cleanliness assertions (no in-tree cache/build dirs; cache root under JARVIS_DATA_DIR) are present in the step
  - The RUNNER_OS = macOS gate lives inside the run block so the Linux leg executes the step and takes the skip path
  - The stale exact-tag pin-variable reference is gone from the workflow (see the Task 2 automated gate); the comment now describes latest-resolution guarding
  - Both path filters include the fixture glob and src/jarvis/**
  - No test files modified in this task (CI-only change)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture source → repo | fixture bytes copied from the upstream org repo at pinned tag v0.3.0 (git-tracked, diffable, no build execution at import time) |
| CI runner → jarvis | the smoke step runs the real index pipeline with runner-provided Xcode and the digest-verified binary from 02-01's installer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-10 | Tampering | fixture provenance | low | mitigate | Adapted (not generated) from the org-owned upstream at tag v0.3.0; committed to git so any later mutation is reviewable; contains no scripts or build phases beyond the single tool target |
| T-02-11 | DoS/Spoofing | runner environment assumption (A1: macos-latest is arm64 with Xcode) | low | accept | Documented RESEARCH assumption; if wrong the step fails loudly red (never silently passes) and the fix is a macos-15 pin or Xcode setup action — surfaced as a flagged assumption, not hidden |
| T-02-12 | DoS | API rate-limit flake failing the smoke | low | accept | Inherits 02-01's single-call posture; a 403 surfaces with its HTTP status in the step log; loud flake preferred over silent skip |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry packages in this plan — fixture data + workflow YAML only; the binary the smoke drives was digest-verified at install (02-01) |
</threat_model>

<verification>
- git ls-files tests/fixtures/mini_xcode_repo — the three fixture files tracked
- Structural checks on the workflow (see Task 2 automated gate)
- The authoritative proof is execution: the next push (or a workflow_dispatch) runs the macOS leg end-to-end — install via latest-resolution, index via xcodebuild dispatch, tree-cleanliness assertions
- Full unit suite stays green: uv run python -m pytest -m "not integration" -q -rs
</verification>

<success_criteria>
- D-09: _swift_indexer_cmd compatibility is proven by CI smoke only — no local integration test added
- D-10: the fixture genuinely exercises _prefers_xcodebuild → --build-tool xcodebuild (the v0.2.x regression path) and is proven indexable through jarvis locally before landing
- SWFT-03: the same CI step asserts no indexer cache/build artifacts inside the indexed tree and the cache under the jarvis data dir
</success_criteria>

## Artifacts this phase produces (Plan 02-03)

- tests/fixtures/mini_xcode_repo/ — fixture paths: scip-swift-test.xcodeproj/project.pbxproj, scip-swift-test.xcodeproj/project.xcworkspace/contents.xcworkspacedata, scip-swift-test/SwiftFile.swift (data for CI smoke and manual verification; no test code)
- .github/workflows/setup-smoke.yml — new workflow step "Index the Swift fixture through jarvis (macOS only)" (post-install jarvis index on the git-init'd fixture copy with ci-bin on PATH, JARVIS_DATA_DIR under RUNNER_TEMP, tree-cleanliness + cache-location assertions, scip-swift --version echo); refreshed install-step comment; extended push/PR path filters ("tests/fixtures/mini_xcode_repo/**", "src/jarvis/**")
- No new env vars, CLI flags, or Python symbols

## Flagged Assumptions (specless probe — unresolved edges, never auto-resolved)

| Requirement | Probe state | Flagged assumption made while planning |
|-------------|-------------|----------------------------------------|
| SWFT-01 (verification half) | unclassified / unresolved | "compatibility verified against the pinned release" = the CI macOS smoke (D-09) + this task's local validation run. RESEARCH A1 (macos-latest = arm64 with full Xcode) is asserted by the repo's own workflow comments but not exercised live in this session — the first real CI run of the new step is the resolving evidence. If it fails on runner tooling (not jarvis code), the fix is a macos-15 runner pin or an Xcode setup action. |
| SWFT-03 (CI assertion half) | unclassified / unresolved | In-tree cleanliness is asserted for .scip-cache and .build only; DerivedData-in-tree is not asserted in CI (xcodebuild's derived data lives inside the cache dir since 02-01, and the one-shot xcshareddata write is accepted per resolution #2 — asserting its absence would make the smoke flake by design). |

<output>
Create `.planning/phases/02-scip-swift-toolchain-update/02-03-SUMMARY.md` when done
</output>
