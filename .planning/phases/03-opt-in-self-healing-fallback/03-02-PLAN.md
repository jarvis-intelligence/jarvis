---
phase: 03-opt-in-self-healing-fallback
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/jarvis/server.py
  - src/jarvis/index_cli.py
  - tests/test_server_tools.py
  - tests/test_index_cli.py
  - README.md
autonomous: true
requirements: [FALL-01]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "FALL-01 (visibility): getIndexStatus on a degraded repo reports capabilities.navigation.available false with reason equal to the persisted status_reason (cause) and a recovery carrying the fallback verb; last_index_run reads outcome='degraded', origin='fallback', reason, recovery (CONTEXT Area 1, MCP contract)."
    - "FALL-01 (nav errors): a navigation tool failing on a degraded repo returns an error payload with state='fallback', cause=<status_reason>, and recovery keys — slotting into the phase-1 D-14 payload contract with zero reshaping (CONTEXT Area 1)."
    - "FALL-01 (CLI): jarvis list renders a degraded row as ◐ with the reason in the 6th TSV field; jarvis status prints origin: fallback, cause: <reason>, and the recovery line naming jarvis reindex (CONTEXT Area 1)."
  artifacts:
    - src/jarvis/server.py
    - src/jarvis/index_cli.py
    - tests/test_server_tools.py
    - tests/test_index_cli.py
    - README.md
  key_links:
    - "server._capability_fields DEGRADED_STATUS branch -> registry.recovery_for ORIGIN_FALLBACK verb (picked up automatically via the existing nav_recovery line)"
    - "index_cli._cmd_list glyph/6th-field branches -> registry.DEGRADED_STATUS constant"
    - "server._error_payload origin keys -> degraded row's status_origin='fallback' (no _error_payload change required)"
  prohibitions:
    - "Never reshape the phase-1 payload contracts — the only server.py behavioral change is the navigation.reason degraded branch; no new keys, no new outcome enum (the registry status string flows verbatim)."
    - "Never add degraded prose branches to _error_payload beyond what the structured state/cause/recovery keys already carry (zero-reshaping is the locked contract; OQ-5 resolved as skip)."
    - "status_stderr must never enter an MCP payload (phase-1 key-by-key payload builder keeps it out; keep it that way)."
    - "jarvis list's 5-column TSV contract must never change — the degraded glyph prefixes the status field and the reason rides a 6th field only."
---

<objective>
Make the degraded state visible on every reporting surface: the one real server.py gap (capabilities.navigation.reason for degraded rows) plus the jarvis list glyph/6th-field rendering; pin jarvis status and the MCP last_index_run/_error_payload flows that Phase 1 already carries for free.

Purpose: A degraded publish that nobody can see re-creates the silent-degradation anti-pattern this milestone exists to fix — reporting is the safety valve that makes opt-in fallback acceptable (ROADMAP Phase 3 dependency note).

Output: server.py navigation.reason degraded branch with tests; index_cli._cmd_list degraded rendering with tests; status/error-payload/last_index_run pins.

D-IDs implemented: Area 1 (MCP last_index_run.outcome='degraded', capabilities.navigation.reason names the actual failure cause, nav-tool state='fallback', zero payload reshaping; list renders ◐ with reason in the 6th field).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-opt-in-self-healing-fallback/03-CONTEXT.md
@.planning/phases/03-opt-in-self-healing-fallback/03-RESEARCH.md
@.planning/phases/03-opt-in-self-healing-fallback/03-PATTERNS.md
@.planning/phases/01-registry-foundation-degradation-reporting/01-03-SUMMARY.md

@src/jarvis/server.py
@src/jarvis/index_cli.py
@src/jarvis/registry.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: MCP degraded reporting — navigation.reason branch + payload pins</name>
  <files>src/jarvis/server.py, tests/test_server_tools.py</files>
  <read_first>
  - src/jarvis/server.py:1-50 (imports — the jarvis.registry import block), 52-80 (_registry_entry), 144-217 (_capability_fields, esp. the navigation-unavailable else-branch at 183-190), 220-268 (_error_payload)
  - src/jarvis/registry.py (DEGRADED_STATUS, ORIGIN_FALLBACK, recovery_for from Plan 03-01)
  - tests/test_server_tools.py:304-327 (JARVIS_DATA_DIR isolation + _error_payload search-only analog), 449-469 (last_index_run analog), 479-496 (navigation-unavailable analog), 502-539 (failed-run-with-live-pointer analog)
  </read_first>
  <behavior>
  - Test 1 (the real gap, RED): with JARVIS_DATA_DIR isolated to tmp_path, upsert a row (status=DEGRADED_STATUS, status_origin=ORIGIN_FALLBACK, status_reason="indexer crashed"); server.get_index_status(repo=...) reports capabilities.navigation.available false, reason == "indexer crashed" (the actual failure cause — today this is None because the branch only predicates on entry.search_only), and recovery containing "jarvis reindex".
  - Test 2 (reason default): same row without status_reason — reason falls back to the default degraded wording "indexer failure — degraded to search-only".
  - Test 3 (last_index_run): same row with a reason — result["last_index_run"] reads outcome "degraded", origin "fallback", the reason, and the fallback recovery verb (flows through the phase-1 verbatim-status contract; pin it).
  - Test 4 (_error_payload): server._error_payload(repo, IndexNotFoundError("no pointer")) on the degraded row carries payload["state"] == "fallback", payload["cause"] == the reason, payload["recovery"] present, and an "error" key — pin that the origin-key block already covers degraded rows with no _error_payload change.
  </behavior>
  <action>
  RED first: write the four tests; Test 1 fails today (reason is None for degraded rows — Pitfall 6 in 03-RESEARCH.md); Tests 2-4 may already pass through the phase-1 additive contracts and serve as pins.
  GREEN in src/jarvis/server.py — exactly one behavioral change:
  - Extend the jarvis.registry import block with DEGRADED_STATUS.
  - In _capability_fields' navigation-unavailable else-branch (183-190), insert between the entry.search_only branch and the entry-is-None branch: elif entry is not None and entry.status == DEGRADED_STATUS: nav_reason = entry.status_reason or "indexer failure — degraded to search-only". The existing nav_recovery = recovery_for(entry) line then carries the fallback verb automatically. Leave _error_payload and the last_index_run spread untouched (degraded flows through verbatim per D-13/D-14/D-15).
  </action>
  <verify>
    <automated>uv run pytest tests/test_server_tools.py -k "degraded or fallback" -q && uv run pytest tests/test_server_tools.py -q</automated>
  </verify>
  <acceptance_criteria>
  - A test asserts get_index_status on a degraded row returns navigation.reason equal to the persisted status_reason and a recovery string containing "jarvis reindex".
  - A test asserts the default reason wording "indexer failure — degraded to search-only" when status_reason is None.
  - A test asserts last_index_run reads outcome "degraded" and origin "fallback".
  - A test asserts _error_payload carries state "fallback", cause, and recovery keys for a degraded repo.
  - git diff on src/jarvis/server.py shows only the DEGRADED_STATUS import and the one elif branch (no other behavioral change).
  - uv run pytest tests/test_server_tools.py -q exits 0.
  </acceptance_criteria>
  <done>MCP surfaces report degraded state with cause and recovery: navigation.reason names the actual failure (gap closed), last_index_run and _error_payload pinned as carrying degraded/fallback verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CLI degraded rendering — list glyph, 6th field, status pin</name>
  <files>src/jarvis/index_cli.py, README.md, tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:1017-1075 (_cmd_list glyph block + 6th-field branch; _cmd_status origin/cause/recovery block)
  - src/jarvis/registry.py (DEGRADED_STATUS from Plan 03-01)
  - tests/test_index_cli.py — existing _cmd_list/_cmd_status tests (grep "def test.*list\|def test.*status" for the capsys patterns; degraded rows are a new status value, same rendering surface)
  </read_first>
  <behavior>
  - Test 1 (glyph + 6th field, RED): a registry row with status=DEGRADED_STATUS and status_reason="scip-python crashed mid-build" renders in _cmd_list output as a line starting with the slug, containing "◐ degraded", and carrying the reason as the 6th tab-separated field (capsys).
  - Test 2 (status command pin): _cmd_status on the same row prints origin: fallback, cause: scip-python crashed mid-build, and a recovery line containing "jarvis reindex" (already origin-driven from Phase 1 — pin, expected green).
  - Test 3 (search-only unchanged): an existing search-only row still renders ◐ with exactly 5 fields (the degraded change adds a branch; search-only rows keep their current shape).
  </behavior>
  <action>
  RED first: Test 1 fails today (a degraded row falls to the ✓ else-branch and gains no 6th field).
  GREEN in src/jarvis/index_cli.py _cmd_list (1017-1038):
  - Glyph block: add elif repo.status == DEGRADED_STATUS: marker = "◐" between the SEARCH_ONLY_STATUS branch and the else ✓.
  - 6th-field block: extend so a degraded row appends f"\t{repo.status_reason or repo.status}" (failed keeps its existing branch verbatim; search-only rows stay 5-field). Update the D-08 comment (1021-1024) to mention degraded joining the ◐ family with a reason field.
  - Docs rider (research A5): add JARVIS_FALLBACK_SEARCH_ONLY to README.md's JARVIS_* env-var list (around line 318-322) with the strict-truthy note, and mention the --fallback-search-only flag in the CLI usage docs where --search-only is described. Match the existing entry style; no new section.
  </action>
  <verify>
    <automated>uv run pytest tests/test_index_cli.py -k "list or status" -q && uv run pytest -m "not integration" -rs -q</automated>
  </verify>
  <acceptance_criteria>
  - A capsys test asserts a degraded row's _cmd_list line contains "◐ degraded" and a 6th field equal to the status_reason.
  - A capsys test asserts _cmd_status prints "origin: fallback", a cause line, and a recovery line containing "jarvis reindex".
  - A test asserts search-only rows keep exactly 5 tab-separated fields (no 6th field regression).
  - README.md's env-var list contains a JARVIS_FALLBACK_SEARCH_ONLY entry naming the accepted values (1/true/yes/on) and the treated-as-off warning behavior.
  </acceptance_criteria>
  <done>jarvis list renders degraded rows as ◐ with the reason in the 6th field, jarvis status prints origin/cause/recovery for fallback, and search-only rendering is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registry row → MCP payload | persisted status_reason/status_origin (sourced from external indexer stderr) flows into JSON payloads and terminal output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-04 | Tampering | _capability_fields/_cmd_list rendering of status_reason (external stderr text) | low | accept | Phase-1 stance (D-02 fidelity): printed verbatim as data, never shell-interpolated or executed; JSON-serializable strings only |
| T-3-05 | Information Disclosure | status_stderr in MCP payloads | low | mitigate | Keep status_stderr out of MCP payloads (phase-1 key-by-key payload builder; this plan adds no key that carries it) |
| T-3-SC | Tampering | package installs | low | accept | Zero packages installed — read-only branch additions over existing code |
</threat_model>

<verification>
- uv run pytest tests/test_server_tools.py tests/test_index_cli.py -k "degraded or fallback" -q — all degraded reporting surfaces.
- uv run pytest -m "not integration" -rs -q — whole unit suite green.
</verification>

<success_criteria>
- getIndexStatus explains a degraded repo: navigation unavailable, reason = actual failure cause, recovery = fallback verb.
- last_index_run and nav-tool error payloads carry degraded/fallback verbatim (pinned).
- jarvis list/status render the degraded state per the locked ◐ + 6th-field contract.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

Plan 03-02 (this plan):
- server.py: DEGRADED_STATUS import + the _capability_fields navigation.reason degraded branch (default wording "indexer failure — degraded to search-only"); no new public symbols otherwise
- index_cli.py: _cmd_list degraded glyph (◐) and degraded 6th-field branches
- README.md: JARVIS_FALLBACK_SEARCH_ONLY env-var entry + --fallback-search-only CLI mention (docs rider, research A5)
</artifacts_produced>

<output>
Create `.planning/phases/03-opt-in-self-healing-fallback/03-02-SUMMARY.md` when done
</output>
