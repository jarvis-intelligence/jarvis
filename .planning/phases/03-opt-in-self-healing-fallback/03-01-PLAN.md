---
phase: 03-opt-in-self-healing-fallback
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jarvis/registry.py
  - src/jarvis/config.py
  - src/jarvis/index_cli.py
  - tests/test_registry.py
  - tests/test_config.py
  - tests/test_index_cli.py
autonomous: true
requirements: [FALL-01, FALL-02, FALL-03, FALL-04]
estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "FALL-01: With fallback resolved on via the CLI flag, an induced post-build-start indexer failure publishes a Zoekt search index and index_repo returns the slug (exit 0 path); the terminal registry row reads status='degraded', status_origin='fallback', status_reason=<first non-empty failure line>, status_stderr=<full failure text>, search_only=False, commit_sha=<attempt sha> (CONTEXT Area 1 + Area 3)."
    - "FALL-04: Missing-binary (MissingBinaryError), bash-shim (both _BASH_SHIM_TOKENS present in the failure text), version-floor (check_scip_version / check_scip_swift_version), language-detection, and duplicate-slug failures all raise with a failed/failed_hard row and nothing published, even with fallback enabled (CONTEXT Area 3 boundary)."
    - "FALL-02: Resolution precedence CLI > persisted > env > off holds across the full 12-cell matrix (CLI on/off/absent × persisted on/off/NULL × env on/off); only the explicit CLI value is ever persisted — a run with no CLI flag leaves fallback_enabled NULL (CONTEXT Area 2, Pitfall 1)."
    - "FALL-03 adjacency: Degraded and search-only stay separate states — degraded rows keep search_only=False so every later run retries the full build; a signature-matched failure on an opted-in repo still ends search_only=1 / origin='signature'; a successful reindex of a degraded repo ends status='indexed' with status_origin/status_reason/status_stderr all NULL (D-04 clearing)."
    - "FALL-01 ordering: A degraded publish whose zoekt step fails leaves the previously-published current pointer intact and the row failed/failed_hard — search is published before SCIP artifacts are retired (ROADMAP Phase 3 note)."
    - "FALL-03 empty/null: A NULL persisted flag defers to the env tier; JARVIS_FALLBACK_SEARCH_ONLY accepts exactly 1/true/yes/on case-insensitively, warns once to stderr on any other value and reads as off; unset reads as off (CONTEXT Area 2)."
    - "FALL-03 ordering/opt-out: --no-fallback-search-only on an already-degraded repo makes the next still-broken run a hard failure — the degraded state never traps (CONTEXT Area 2)."
    - statement: "Concurrency: a watch reindex racing a manual index on the same registry relies on the existing busy_timeout=5000 retry rather than corruption; an interrupted run may strand a transitional 'indexing' row (WR-01 accepted edge from Phase 1 UAT, mitigated here because the next reindex always retries the full build)."
      verification: backstop
  artifacts:
    - src/jarvis/registry.py
    - src/jarvis/config.py
    - src/jarvis/index_cli.py
    - tests/test_registry.py
    - tests/test_config.py
    - tests/test_index_cli.py
  key_links:
    - "index_cli._resolve_fallback -> config.fallback_search_only_from_env (env tier read exactly once per run, and only from config.py)"
    - "degrade gate in index_repo's main-pipeline except -> _publish_search_only (corrected zoekt-before-retire order) -> terminal upsert(status=DEGRADED_STATUS, status_origin=ORIGIN_FALLBACK, status_stderr=<full text>)"
    - "registry.set_fallback_enabled call sites <- the two transitional 'indexing' upserts in index_repo (only when the CLI value is explicit)"
    - "degraded terminal write's commit_sha -> the FALL-05 attempt-sha key consumed by Plan 03-03's _watch_should_retry_full_build"
  prohibitions:
    - "Never persist the resolved fallback bool — only the explicit CLI value may be written (a resolved-bool write collapses NULL to 0 on the first env-off run and locks a later env-on out; persisted outranks env)."
    - "Never set search_only=True on a degraded row — it is the one-way trap only jarvis forget escapes, and it kills self-heal."
    - "Never retire SCIP artifacts before the zoekt publish succeeds inside _publish_search_only."
    - "Never add degrade handling to the pre-pipeline wrap, and never fork a second publish path beside _publish_search_only."
    - "Never accept a JARVIS_FALLBACK_SEARCH_ONLY value outside 1/true/yes/on (case-insensitive) as on."
    - "Never read JARVIS_FALLBACK_SEARCH_ONLY outside config.py (env reads live in config per the JARVIS_DATA_DIR house pattern)."
    - "Never add a --fallback-search-only flag to the reindex parser — reindex honors the persisted value silently."
    - "Terminal upserts must never reset the persisted fallback_enabled value (upsert's ON CONFLICT list leaves it alone; writes go through set_fallback_enabled only)."
    - "jarvis index must never skip the full build for a degraded row — the sha-keyed skip is a watch-driver-only policy (FALL-03 vs FALL-05)."
---

<objective>
Wire the opt-in self-healing fallback into the indexing pipeline: tri-state resolution (CLI > persisted > env > off), the degrade branch in index_repo's main-pipeline failure handler, the corrected zoekt-before-retire publish ordering, and the registry/config carriers the degraded state rides on.

Purpose: This is the behavioral core of the milestone — with fallback enabled, a post-build-start indexer failure degrades the repo to a working search-only index instead of leaving it with nothing, and every later reindex automatically retries the full build (FALL-01..04).

Output: registry carriers (DEGRADED_STATUS, ORIGIN_FALLBACK, fallback_enabled column, set_fallback_enabled, upsert status_stderr param, recovery_for branch), config.fallback_search_only_from_env, and the index_cli resolution + degrade branch + index-parser flag, each pinned by unit tests.

D-IDs implemented: Area 1 (degraded run outcome & reporting row), Area 2 (opt-in surface & flag semantics), Area 3 (self-heal & retry mechanics, post-build-start boundary, degraded-publish-failure hard failure, recovery wording).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-opt-in-self-healing-fallback/03-CONTEXT.md
@.planning/phases/03-opt-in-self-healing-fallback/03-RESEARCH.md
@.planning/phases/03-opt-in-self-healing-fallback/03-PATTERNS.md
@.planning/phases/01-registry-foundation-degradation-reporting/01-01-SUMMARY.md

@src/jarvis/registry.py
@src/jarvis/index_cli.py
@src/jarvis/config.py
</context>

<assumption_delta_decision>
The assumption-delta detector fired on pluralization of "fallback" in the phase title (Phase 3 adds a SECOND degradation mechanism beside manual/signature search-only). Disposition: **add-alongside**. The CONTEXT.md tri-state/taxonomy decisions are authoritative: the new mechanism is a distinct self-healing state — status='degraded' + origin='fallback' via new constants DEGRADED_STATUS/ORIGIN_FALLBACK — added alongside the existing manual/signature permanent search_only=1 paths, which stay primary and unchanged for their own triggers. "fallback" is now a primary noun (the origin slug), not a secondary alternative; existing search-only semantics and payloads are untouched (zero reshaping). Reusing search_only=1 for the generic fallback is explicitly out of scope (REQUIREMENTS Out of Scope).
</assumption_delta_decision>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Registry + config carriers for the degraded state</name>
  <files>src/jarvis/registry.py, src/jarvis/config.py, tests/test_registry.py, tests/test_config.py</files>
  <read_first>
  - src/jarvis/registry.py (full, 311 lines — constants 18-29, _SCHEMA 31-49, _ensure_column 52-68, RegisteredRepo 82-124, recovery_for 137-150, __init__ migrations 153-172, upsert 174-220, record_failure 229-265, get/list SELECTs 274-291, mark_tracked_files 293-303)
  - src/jarvis/config.py (92 lines — data_dir env-read house pattern at 62-66, imports at 12-16)
  - tests/test_registry.py:212-261, 316-327, 369-416 (roundtrip / legacy-DB migration / _entry helper / recovery_for analogs)
  - tests/test_config.py (87 lines — env-var test style, monkeypatch.setenv/delenv)
  - .planning/phases/03-opt-in-self-healing-fallback/03-PATTERNS.md (registry.py + config.py sections)
  </read_first>
  <behavior>
  - Test 1 (migration): a pre-phase-3 database built with raw sqlite3 (pattern of test_search_only_column_migrates_onto_an_existing_database, tests/test_registry.py:238-261) gains the fallback_enabled column on first Registry open; the legacy row reads fallback_enabled is None (NULL = defer to env).
  - Test 2 (tri-state roundtrip): set_fallback_enabled(slug, True) then get() returns fallback_enabled True; set False returns False; on a fresh row the default is None; a subsequent plain upsert (terminal-write shape, no fallback argument) leaves the stored value untouched.
  - Test 3 (recovery wording): recovery_for on an entry with status_origin=ORIGIN_FALLBACK returns the locked string "fix the indexer failure, then `jarvis reindex mine` (full build retries automatically)" (slug interpolated).
  - Test 4 (stderr carrier + D-04): upsert accepts a status_stderr keyword and get() round-trips the full multi-line text; a subsequent plain upsert on the same row (no status_origin/status_reason/status_stderr arguments) NULL-clears all three — D-04 semantics hold for previously-degraded rows.
  - Test 5 (env matrix, parametrized in tests/test_config.py): JARVIS_FALLBACK_SEARCH_ONLY of 1, true, yes, on, TRUE, On each yield True from config.fallback_search_only_from_env(); unset yields False; a garbage value (e.g. "maybe" or "2" or empty string) yields False AND prints exactly one warning line to stderr (capsys) naming the bad value and the accepted set.
  </behavior>
  <action>
  RED first: write the five tests above; confirm they fail (AttributeError/ImportError for missing symbols is a valid first red).
  GREEN in src/jarvis/registry.py:
  - Add DEGRADED_STATUS = "degraded" beside SEARCH_ONLY_STATUS (line 21) and ORIGIN_FALLBACK = "fallback" beside the ORIGIN_* block (lines 27-29); extend the adjacent taxonomy comment (lines 23-26) so the reserved-slot note now describes the shipped constants.
  - Add _ensure_column(self._conn, "fallback_enabled", "INTEGER") in Registry.__init__ after the status_stderr call (line 172). Leave _SCHEMA untouched — _ensure_column is the house pattern for additive columns.
  - Add RegisteredRepo field fallback_enabled: bool | None = None (after status_stderr, line 101) with a why-comment (NULL = never set, defer to env). Extend _row_to_repo's unpack and constructor call, and append fallback_enabled to the SELECT column lists in get() (276-279) and list() (286-289) so all three stay positionally in sync.
  - Extend upsert with keyword status_stderr: str | None = None after status_reason: add status_stderr to the INSERT column list, its VALUES placeholder, and the parameter tuple; include it in the returned RegisteredRepo. The existing ON CONFLICT line status_stderr=excluded.status_stderr now carries the passed value; the None default preserves D-04 clearing. Update the D-04 comment (lines 201-205), which currently states status_stderr has no upsert parameter at all.
  - Add recovery_for branch for ORIGIN_FALLBACK between the ORIGIN_SIGNATURE and ORIGIN_MANUAL branches returning f"fix the indexer failure, then `jarvis reindex {entry.slug}` (full build retries automatically)" (locked wording, CONTEXT Area 3).
  - Add method set_fallback_enabled(self, slug: str, value: bool) -> None: one parameterized UPDATE repos SET fallback_enabled = ? WHERE slug = ? plus commit, mirroring mark_tracked_files (293-303); docstring explains why it is deliberately not an upsert column — upsert's full-overwrite ON CONFLICT list would reset the tri-state on every transitional write, and persisting the RESOLVED bool would collapse NULL to 0 (Pitfall 1).
  GREEN in src/jarvis/config.py:
  - Add import sys beside import os (line 14).
  - Add _FALLBACK_TRUTHY = frozenset({"1", "true", "yes", "on"}) and public function fallback_search_only_from_env() -> bool following the data_dir env-read house pattern: raw = os.environ.get("JARVIS_FALLBACK_SEARCH_ONLY"); None returns False; raw.strip().lower() in _FALLBACK_TRUTHY returns True; any other value prints one line to stderr — warning: JARVIS_FALLBACK_SEARCH_ONLY={raw!r} is not one of 1/true/yes/on — treating as off — and returns False. Docstring carries the loud-misconfiguration rationale (CONTEXT Area 2).
  </action>
  <verify>
    <automated>uv run pytest tests/test_registry.py tests/test_config.py -q</automated>
  </verify>
  <acceptance_criteria>
  - tests/test_registry.py contains a test asserting a legacy pre-phase-3 DB row reads fallback_enabled None after Registry open.
  - tests/test_registry.py contains a tri-state roundtrip test (None default; True; False; plain upsert leaves the stored value).
  - recovery_for test asserts the exact locked string fix the indexer failure, then `jarvis reindex mine` (full build retries automatically) for origin fallback.
  - An upsert status_stderr roundtrip test exists and a D-04 test proves a subsequent plain upsert NULL-clears status_origin, status_reason, and status_stderr on a previously-degraded row.
  - tests/test_config.py contains a parametrized strict-truthy matrix test (1/true/yes/on/TRUE/On → True; unset → False; garbage → False with exactly one stderr warning line via capsys).
  - uv run pytest tests/test_registry.py tests/test_config.py -q exits 0.
  - registry.py contains exactly one new _ensure_column call for fallback_enabled and _SCHEMA is byte-identical to before (git diff shows no _SCHEMA change).
  </acceptance_criteria>
  <done>All carrier tests green: migration NULL default, tri-state roundtrip, locked recovery wording, status_stderr carrier with D-04 clearing preserved, strict env matrix with warn-once; full registry+config files pass.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: End-to-end degraded publish — resolution, gate, branch, ordering</name>
  <files>src/jarvis/index_cli.py, tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:315-370 (_git_head, _run and its FileNotFoundError translation), 570-612 (_resolve_* family), 670-734 (_retire_scip_artifacts, _publish_search_only), 771-998 (index_repo: pre-pipeline wrap 816-870, search-only branch 872-897, main pipeline + signature fallback 899-931, terminal success 972-984, main except 985-996), 1001-1014 (_cmd_index), 1256-1328 (build_parser)
  - src/jarvis/registry.py (Task 1 result — DEGRADED_STATUS, ORIGIN_FALLBACK, upsert status_stderr)
  - tests/test_index_cli.py:33-44 (_fake_completed_process, _init_git_repo), 456-470 (end-to-end fake-success analog), 1796-1828 (signature-fallback mock pattern), 2658-2693 (pre-pipeline gate analog)
  - .planning/phases/03-opt-in-self-healing-fallback/03-PATTERNS.md (index_cli.py section)
  </read_first>
  <behavior>
  - Test 1 (FALL-01 happy path, the tracer): copytree FIXTURE_REPO to tmp, _init_git_repo; monkeypatch check_scip_version to a no-op, _run_semantic_stage to return False, and _run to raise IndexingError("error: simulated post-build-start failure") when step.endswith(" index") else return _fake_completed_process(cmd) (house pattern at tests/test_index_cli.py:1796-1828); index_repo(repo_dir, root=data_root, fallback_search_only=True) returns the slug; the registry row reads status == "degraded", status_origin == "fallback", status_reason == "error: simulated post-build-start failure", status_stderr == the full text, search_only is False, commit_sha == the repo's current HEAD sha.
  - Test 2 (stderr contract): capsys on the same scenario shows exactly one warning line containing "degraded to search-only" and the reason (the run path is a success — returns slug, no raise).
  - Test 3 (FALL-04 missing binary): _run raising MissingBinaryError on the indexer step with fallback on still raises from index_repo; the row reads status "failed" / origin "failed_hard". Plus a direct unit test that real _run translates FileNotFoundError into MissingBinaryError (call _run with a nonexistent command name).
  - Test 4 (FALL-04 bash-shim): _run raising IndexingError whose text contains both tokens LAUNCHER_ARGS[@] and unbound variable, with fallback on, still raises; row failed/failed_hard.
  - Test 5 (FALL-04 pre-pipeline): check_scip_version raising RuntimeError (match "below the required floor" pattern of tests/test_index_cli.py:2658-2693) with fallback_search_only=True still raises; row failed/failed_hard — the degrade branch is unreachable for pre-pipeline failures.
  - Test 6 (FALL-01 ordering fix): two-phase mock — first run all _run steps succeed (mirror the fake-success shape at tests/test_index_cli.py:456-470) so a full publish writes config.index_dir(slug, data_root)/current; second run the indexer step fails AND the zoekt-git-index step fails, with fallback on: index_repo raises, the row reads failed/failed_hard, and the current pointer file written by the first run still exists (zoekt published before SCIP artifacts are retired).
  </behavior>
  <action>
  RED first: write Tests 1-6; Tests 1-2 fail on the missing degrade path (index_repo raises instead of degrading), Test 3 on the missing MissingBinaryError symbol, Test 6 on the current retire-first ordering destroying the pointer.
  GREEN in src/jarvis/index_cli.py:
  - Imports: add DEGRADED_STATUS and ORIGIN_FALLBACK to the jarvis.registry import block (lines 1-33).
  - Add class MissingBinaryError(IndexingError) directly after class IndexingError (line 159) with a one-line why-docstring (degrade-gate exclusion: a missing binary is a setup.sh problem, and the fallback must keep it a loud failure). Change _run's FileNotFoundError handler (lines 366-370) to raise MissingBinaryError with the same message text (the subclass IS-A IndexingError, so every existing except IndexingError site keeps working).
  - Add _resolve_fallback(registry: Registry, slug: str, cli: bool | None) -> bool beside _resolve_search_only (lines 570-612): cli is not None returns cli; an existing row with fallback_enabled not None returns it; otherwise return config.fallback_search_only_from_env(). Docstring states the locked precedence CLI > persisted > env > off and that the resolved bool is never written back (the persistence deviation from the _resolve_scheme idiom).
  - index_repo signature (line 771): add keyword fallback_search_only: bool | None = None. Resolve it inside the pre-pipeline wrap beside search_only (after check_scip_version, near line 823): fallback_enabled = _resolve_fallback(registry, slug, fallback_search_only).
  - Degrade gate — insert between the reason extraction (lines 990-991) and record_failure (line 992) in the main-pipeline except handler: guard condition (fallback_enabled and not isinstance(exc, MissingBinaryError) and not _bash_shim_failure(text)); inside, a nested try wraps _publish_search_only(repo_path, slug, root, semantic_include) then the terminal write registry.upsert(slug, str(repo_path), language, sha, DEGRADED_STATUS, scheme_override=scheme, semantic_include=semantic_include, language_override=language_override, status_origin=ORIGIN_FALLBACK, status_reason=reason, status_stderr=text) — search_only stays at its False default — then registry.mark_tracked_files(slug, tracked) and conditional registry.mark_semantic_indexed(slug) (mirroring the signature-fallback terminal write at lines 923-930). Nested except Exception: registry.record_failure(slug, str(repo_path), language, ORIGIN_FAILED_HARD, reason, text) then raise IndexingError(str(exc)) from exc — the degraded publish failing voids the fallback promise (CONTEXT Area 3), and the row keeps the ORIGINAL indexer cause as the actionable reason. On success print exactly one stderr line — warning: {slug} degraded to search-only — {reason}. Navigation tools are unavailable; the next reindex retries the full build. — then return slug. Leave the pre-pipeline wrap and the search-only branch handler (885-894) untouched by degrade logic.
  - Corrected publish ordering in _publish_search_only (lines 703-734): move the _retire_scip_artifacts(slug, root) call from the top to AFTER _warn_on_coverage_shortfall and _sweep_zoekt_tmp_orphans (i.e., after the zoekt publish provably succeeded) and BEFORE _run_semantic_stage; update the docstring paragraph that currently says "First retires any previously published SCIP index" to state the publish-then-retire rationale (a zoekt failure must leave an existing index untouched). All three callers (manual --search-only, signature, degrade) get the fix through this one path.
  - Parser: index_parser gains --fallback-search-only with action=argparse.BooleanOptionalAction and default=None (generates the --no- pair; do not use store_true), help text: on a post-build-start indexer failure, publish search-only and retry the full build on the next reindex (persisted per-repo; JARVIS_FALLBACK_SEARCH_ONLY sets the global default). _cmd_index forwards fallback_search_only=getattr(args, "fallback_search_only", None) in its index_repo call (line 1004-1009).
  </action>
  <verify>
    <automated>uv run pytest tests/test_index_cli.py -k "degraded or fallback or hard_failure or preserves" -q && uv run pytest -m "not integration" -rs -q</automated>
  </verify>
  <acceptance_criteria>
  - tests/test_index_cli.py contains a test where index_repo(repo, root=..., fallback_search_only=True) under an induced indexer-step failure returns the slug and leaves a row with status "degraded", status_origin "fallback", status_reason equal to the first failure line, status_stderr equal to the full failure text, search_only False, and commit_sha equal to the repo HEAD.
  - A capsys test asserts one stderr warning line containing "degraded to search-only" on the degraded path.
  - Three boundary tests prove missing-binary (MissingBinaryError), bash-shim (both tokens), and pre-pipeline version-floor failures raise with row status "failed" and status_origin "failed_hard" while fallback is enabled.
  - A test proves real _run raises MissingBinaryError (not plain IndexingError) on FileNotFoundError.
  - A two-phase test proves a degraded publish with a failing zoekt step leaves the previously written index_dir current pointer in place and the row failed/failed_hard.
  - In _publish_search_only, the _retire_scip_artifacts call textually follows the zoekt _run call (git diff shows the move).
  - index_parser declares --fallback-search-only with argparse.BooleanOptionalAction and default=None.
  - uv run pytest -m "not integration" -rs -q exits 0 (full unit suite, no regressions).
  </acceptance_criteria>
  <done>The single happy degrade path works end-to-end (induced failure → degraded row + exit-0 return + one warning), the three FALL-04 exclusion classes stay loud hard failures, the publish ordering protects an existing index, and the full unit suite is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persistence wiring, precedence matrix, self-heal pins</name>
  <files>src/jarvis/index_cli.py, tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:816-900 (pre-pipeline wrap resolution order; the two transitional indexing upserts at 872-875 and 899-900)
  - src/jarvis/registry.py (set_fallback_enabled from Task 1)
  - tests/test_index_cli.py:348-356 (allows-reindexing-same-slug pattern), 555-578 (override-persistence analogs), 1796-1828 (two-phase mock pattern)
  - .planning/phases/03-opt-in-self-healing-fallback/03-RESEARCH.md (Pattern 2 persistence deviation; Pitfall 1; Pitfall 4 precedence split-brain)
  </read_first>
  <behavior>
  - Test 1 (precedence matrix, parametrized over _resolve_fallback directly with a real Registry in tmp_path): CLI True/False/None × persisted True/False/None × env on/off — CLI value wins whenever present; else persisted when not None; else env; else off. All 12 cells.
  - Test 2 (persistence — explicit only): a healthy full run with fallback_search_only=True leaves row.fallback_enabled True (setter wired after the transitional upsert).
  - Test 3 (Pitfall 1 — NULL stays NULL): runs with no CLI flag and env unset, and separately with JARVIS_FALLBACK_SEARCH_ONLY=1 set, both leave row.fallback_enabled None — the env tier is consumed at run time only.
  - Test 4 (opt-out immediate): on a degraded repo, index_repo(..., fallback_search_only=False) with a still-failing indexer raises (hard failure) and the row reads failed/failed_hard.
  - Test 5 (self-heal): two-phase mock — first run degrades (fallback on), then the mock is repaired and a second index_repo with no CLI flag (persisted True) ends status "indexed", search_only False, and status_origin/status_reason/status_stderr all None.
  - Test 6 (degrade again on fresh failure): the second run still failing degrades again with a fresh degraded row (retried full build, fresh failure — not a stale cached state).
  - Test 7 (signature precedence): an opted-in repo whose indexer failure matches a _SEARCH_ONLY_SIGNATURES entry ("No SCIP shards found") ends status "search-only", search_only True, status_origin "signature" — the signature path pre-empts the degrade branch.
  </behavior>
  <action>
  RED first: Tests 2-4 fail (set_fallback_enabled is never called from index_repo yet); Tests 1/5/6/7 may pass already from Task 2 — they are the contract pins; keep them regardless.
  GREEN: wire the persistence call sites — immediately after the search-only branch's transitional indexing upsert (lines 873-875) AND immediately after the main branch's transitional upsert (lines 899-900), add: if fallback_search_only is not None: registry.set_fallback_enabled(slug, fallback_search_only). Both sites are required because the branches diverge before their first upsert; pre-pipeline failures intentionally leave the flag unpersisted, matching the --scheme/--language semantics the locked decision references (the retry re-supplies the CLI flag). Fix any resolution/gate gaps the RED tests expose; no other changes.
  </action>
  <verify>
    <automated>uv run pytest tests/test_index_cli.py -k "fallback or precedence or self_heal or degrades_again or preempts" -q && uv run pytest -m "not integration" -rs -q</automated>
  </verify>
  <acceptance_criteria>
  - A parametrized 12-cell precedence test over _resolve_fallback exists and passes (CLI > persisted > env > off).
  - A test proves an explicit CLI True persists fallback_enabled True on the row after a healthy run.
  - A test proves runs without a CLI flag (env unset, and env =1) leave fallback_enabled None on the row.
  - A test proves --no-fallback-search-only (CLI False) on a degraded repo with a still-broken indexer raises and the row reads failed/failed_hard.
  - A two-phase test proves degrade-then-repair ends status "indexed" with status_origin, status_reason, and status_stderr all None.
  - A test proves a signature-matched failure on an opted-in repo ends search_only True / status_origin "signature".
  - uv run pytest -m "not integration" -rs -q exits 0.
  </acceptance_criteria>
  <done>Full tri-state contract pinned: 12-cell matrix green, explicit-only persistence (NULL stays NULL), immediate opt-out, self-heal to indexed with D-04 clearing, fresh-failure re-degrade, and signature precedence over the generic fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → process | JARVIS_FALLBACK_SEARCH_ONLY crosses from the user shell/MCP-spawned environment into run behavior |
| indexer stderr → registry/status_stderr | untrusted external tool output persisted verbatim and later printed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-3-01 | Tampering | registry writes (set_fallback_enabled, upsert status_stderr, degraded terminal write) | medium | mitigate | Parameterized ? SQL only (repo rule, AGENTS.md); failure text persisted as data, never interpolated into SQL |
| T-3-02 | Elevation/Spoofing | JARVIS_FALLBACK_SEARCH_ONLY env read (config.fallback_search_only_from_env) | low | mitigate | Strict frozenset 1/true/yes/on case-insensitive; any other value warns loudly and reads off; the persisted per-repo flag outranks env so a one-time explicit opt-out is durable |
| T-3-03 | Tampering | degraded stderr warning line / status_reason rendering (index_cli print) | low | accept | Phase-1 stance (D-02 fidelity): printed as-is, never shell-interpolated; local-first single-user tool with no execution of persisted text |
| T-3-SC | Tampering | package installs (npm/pip/cargo) | low | accept | Phase installs zero packages — stdlib argparse.BooleanOptionalAction + existing deps only; no legitimacy checkpoint needed |
</threat_model>

<verification>
- uv run pytest tests/test_registry.py tests/test_config.py -q — carriers green.
- uv run pytest tests/test_index_cli.py -k "degraded or fallback or hard_failure or preserves or precedence or self_heal or degrades_again or preempts" -q — full FALL-01..04 coverage.
- uv run pytest -m "not integration" -rs -q — whole unit suite green (CI gate).
- Manual smoke (optional, verification only): JARVIS_FALLBACK_SEARCH_ONLY=1 uv run jarvis index tests/fixtures/mini_py_repo after breaking one importer step produces status degraded + exit 0.
</verification>

<success_criteria>
- With fallback enabled, an induced post-build-start failure degrades to a queryable search index (degraded row, exit 0, one warning) — FALL-01.
- Opt-in is tri-state at CLI/persisted/env with the locked precedence and explicit-only persistence — FALL-02.
- Degraded self-heals on the next run (full build retried; success clears failure fields) and degrades again only on fresh failure — FALL-03.
- Pre-build failures stay hard failures even with fallback on — FALL-04.
- The corrected publish ordering protects previously-published indexes from a failing degraded publish.
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces

Plan 03-01 (this plan):
- registry.py symbols: DEGRADED_STATUS ("degraded"), ORIGIN_FALLBACK ("fallback"), RegisteredRepo.fallback_enabled (bool | None), Registry.set_fallback_enabled(slug, value), upsert(status_stderr=...), recovery_for ORIGIN_FALLBACK branch, repos fallback_enabled INTEGER column (via _ensure_column)
- config.py symbols: fallback_search_only_from_env() -> bool, _FALLBACK_TRUTHY frozenset, JARVIS_FALLBACK_SEARCH_ONLY env var (read-only)
- index_cli.py symbols: MissingBinaryError(IndexingError), _resolve_fallback(registry, slug, cli) -> bool, index_repo(fallback_search_only=...) keyword, the degrade gate + degraded terminal write in index_repo's main-pipeline except, reordered _publish_search_only (retire after zoekt), index_parser --fallback-search-only/--no-fallback-search-only (argparse.BooleanOptionalAction)
- tests: tests/test_registry.py (migration, tri-state roundtrip, recovery wording, stderr carrier + D-04), tests/test_config.py (strict env matrix), tests/test_index_cli.py (degraded happy path, stderr contract, three boundary exclusions, _run translation, pointer preservation, precedence matrix, persistence semantics, self-heal, re-degrade, signature precedence)
</artifacts_produced>

<output>
Create `.planning/phases/03-opt-in-self-healing-fallback/03-01-SUMMARY.md` when done
</output>
