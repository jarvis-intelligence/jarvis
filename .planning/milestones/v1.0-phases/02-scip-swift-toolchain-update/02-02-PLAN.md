---
phase: 02-scip-swift-toolchain-update
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/jarvis/index_cli.py
  - src/jarvis/watch.py
  - tests/test_index_cli.py
  - tests/test_watch.py
autonomous: true
requirements: [SWFT-01, SWFT-03]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "jarvis index on a Swift repo whose scip-swift is older than 0.3.0 fails loudly with an IndexingError naming the installed version, the 0.3.0 floor, and a re-run-setup.sh recovery hint (D-04)"
    - "Non-Swift repos never invoke the scip-swift version probe — the check is gated on the resolved language inside index_repo (D-04)"
    - "An unparseable scip-swift --version string does not block indexing (warn-by-omission, same policy as check_scip_version)"
    - "jarvis forget <slug> removes that repo's cache/scip-swift/<slug>/ directory; forgetting a repo that was never Swift-indexed still succeeds (D-06)"
    - "jarvis watch drops file events whose path contains .scip-cache, .build, DerivedData, .index-store, IndexStore, or .swiftpm as a component, wherever they appear (D-07/D-08)"
    - "Swift source files and .xcodeproj/project.pbxproj edits still trigger reindexes — the ignore extension never suppresses legitimate triggers"
    - "The pre-build floor failure flows through Phase 1's pre-pipeline failure wrap and persists a failed_hard row with the cause (no new wiring required — existing hook)"
  artifacts:
    - path: "src/jarvis/index_cli.py"
      provides: "runtime scip-swift floor gate + forget cache sweep"
      contains: "MIN_SCIP_SWIFT_VERSION"
    - path: "src/jarvis/watch.py"
      provides: "Swift artifact ignore coverage in should_ignore_path"
      contains: "DerivedData"
    - path: "tests/test_index_cli.py"
      provides: "floor-gate tests + forget-sweep test"
    - path: "tests/test_watch.py"
      provides: "Swift artifact ignore cases + non-ignore guards"
  key_links:
    - from: "src/jarvis/index_cli.py index_repo (language == swift branch)"
      to: "src/jarvis/index_cli.py check_scip_swift_version"
      via: "first statement of the swift branch, adjacent to the existing check_scip_version call pattern"
      pattern: "check_scip_swift_version"
    - from: "src/jarvis/index_cli.py _cmd_forget"
      to: "src/jarvis/config.py swift_cache_dir"
      via: "shutil.rmtree(config.swift_cache_dir(slug), ignore_errors=True) after the lancedb sweep"
      pattern: "swift_cache_dir"
    - from: "src/jarvis/watch.py should_ignore_path"
      to: "_IGNORED_PATH_PARTS"
      via: "component-membership check over the extended set — no matcher logic change"
      pattern: "_IGNORED_PATH_PARTS"
  prohibitions:
    - "Watch ignores must never suppress legitimate reindex triggers — no wholesale .xcodeproj or *.swift ignoring; project.pbxproj edits and Swift sources always trigger. The one-shot xcshareddata/swiftpm/configuration event on first index is accepted and documented, NOT ignored (orchestrator resolution #2)"
    - "jarvis forget must never delete anything outside the forgotten slug's own cache/scip-swift/<slug>/ directory — the sweep path is constructed solely from data_dir()/cache/scip-swift/<slug>"
    - "The runtime floor must not run for non-Swift repos and must not block indexing on unparseable version output — warn-by-omission is preserved exactly as in check_scip_version"

---

<objective>
Expansion plan 1 of 2: runtime guards and lifecycle for the new toolchain — a Swift-gated scip-swift version floor inside jarvis index (D-04), the forget-time cache sweep (D-06), and watch self-trigger prevention for Swift build artifacts (D-07/D-08).

Purpose: installs now auto-roll (Plan 02-01), so a too-old binary must fail loudly at index time with a recoverable error rather than silently producing broken indexes; the out-of-repo cache must die with the repo when forgotten; and the watch loop must never reindex in response to files the indexer/build system itself drops in-tree.

Output: check_scip_swift_version + MIN_SCIP_SWIFT_VERSION in index_cli.py, one rmtree line in _cmd_forget, six new entries in watch.py's _IGNORED_PATH_PARTS, and the tests pinning all three.
</objective>

<execution_context>
@/Users/ddphuong/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scip-swift-toolchain-update/02-CONTEXT.md
@.planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md
@.planning/phases/02-scip-swift-toolchain-update/02-PATTERNS.md
@.planning/phases/02-scip-swift-toolchain-update/02-01-SUMMARY.md
@.planning/phases/01-registry-foundation-degradation-reporting/01-02-SUMMARY.md
@src/jarvis/index_cli.py
@src/jarvis/watch.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swift-gated runtime scip-swift version floor mirroring check_scip_version</name>
  <files>src/jarvis/index_cli.py, tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:380-415 (parse_scip_version with its v-optional regex, _scip_version_output isolated for monkeypatching, MIN_SCIP_VERSION + check_scip_version with the IndexingError-and-recovery-hint pattern)
  - src/jarvis/index_cli.py:760-800 (index_repo: the unconditional check_scip_version call site and the language == "swift" branch where the new gate goes — note the Phase-1 pre-pipeline failure wrap that will persist the raise as a failed_hard row)
  - tests/test_index_cli.py:612-658 (test_check_scip_version_rejects_v070 / accepts_v090 / tolerates_unparseable — the exact monkeypatch pattern to mirror)
  - .planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md — Pattern 2 and Pitfall 6 (scip-swift prints "0.3.0 (swift 6.2.4)" — no v prefix; parse_scip_version already tolerates it)
  </read_first>
  <behavior>
  - Test: check_scip_swift_version with _scip_swift_version_output monkeypatched to return "0.2.1 (swift 6.1.0)" raises IndexingError whose message contains "0.2.1", the required "0.3.0", and a re-run setup.sh hint
  - Test: with output "0.3.0 (swift 6.2.4)" it does not raise (real v0.3.0 format, no v prefix)
  - Test: with an unparseable string ("weird build") it does not raise (warn-by-omission)
  - Test: _scip_swift_version_output raising IndexingError naming setup.sh when the binary is missing from PATH (FileNotFoundError wrap, mirroring _scip_version_output)
  - Test: index_repo on a non-Swift repo never calls _scip_swift_version_output (monkeypatch it to fail the test if invoked — the Swift gate is load-bearing)
  </behavior>
  <action>
  RED first: add the five tests per the behavior block, mirroring the existing version-check test style (monkeypatch the isolated output function; pytest.raises with message-content asserts). Commit test(02-02).

  GREEN in src/jarvis/index_cli.py: add module constant MIN_SCIP_SWIFT_VERSION = (0, 3, 0) beside MIN_SCIP_VERSION with a why-comment (installs auto-roll to latest since 02-01, so the runtime gate is the defense against a stale binary left on PATH — the same PATH-shadowing hazard the scip installer's version-gate comment warns about). Add _scip_swift_version() probe functions mirroring the scip pair: _scip_swift_version_output() runs scip-swift --version (subprocess.run capture_output text check=False), wraps FileNotFoundError into IndexingError naming setup.sh, returns stdout+stderr — isolated for tests, docstring says so. check_scip_swift_version() reuses parse_scip_version verbatim (its v-optional regex already parses the v-less scip-swift format — do not write a second parser), returns early on None, and raises IndexingError when below the floor with a message stating the installed version, the >= 0.3.0 requirement, why (releases before 0.3.0 silently ignore --build-tool xcodebuild, so .xcodeproj repos index wrong), and the recovery hint to re-run setup.sh and remove any older scip-swift earlier on PATH. Wire the call as the first statement of the language == "swift" branch inside index_repo, adjacent to (after) the existing check_scip_version call — Swift-gated, never unconditional, because non-macOS hosts legitimately never install scip-swift. No other changes: the raise flows through Phase 1's pre-pipeline wrap and persists a failed_hard row with this cause automatically. Commit feat(02-02).
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_index_cli.py -m "not integration" -q</automated>
  </verify>
  <done>Reject/accept/tolerate/missing-binary/non-swift-gate tests pass; the floor constant is (0, 3, 0); the gate lives inside the swift branch of index_repo</done>
  <acceptance_criteria>
  - src/jarvis/index_cli.py defines MIN_SCIP_SWIFT_VERSION = (0, 3, 0), _scip_swift_version_output(), and check_scip_swift_version() which calls parse_scip_version (no duplicate parser)
  - The check_scip_swift_version call site sits inside the language == "swift" branch of index_repo, not at module import or in the unconditional prelude
  - Error message contains the installed version, "0.3.0", and "setup.sh"
  - A non-Swift index path is proven not to invoke the probe by a sentinel test
  - TDD gate: test(02-02) commit precedes feat(02-02)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: watch ignores Swift build artifacts wherever they appear, without over-ignoring</name>
  <files>src/jarvis/watch.py, tests/test_watch.py</files>
  <read_first>
  - src/jarvis/watch.py:18-24 (_IGNORED_PATH_PARTS set and should_ignore_path component-membership matcher — note "build" without the dot is already covered; ".build" is a distinct SwiftPM entry)
  - tests/test_watch.py:91-98 (test_should_ignore_path_skips_vendor_and_git_dirs — the pure-function test pattern to extend)
  - .planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md — Pitfall 7 (the one-shot xcshareddata write: accepted per orchestrator resolution #2, documented, not ignored) and the D-08 belt-and-suspenders framing (cache moved out-of-tree, but xcodebuild/SwiftPM can still drop artifacts in-tree)
  </read_first>
  <behavior>
  - Test: should_ignore_path returns True for paths containing any of .scip-cache, .build, DerivedData, .index-store, IndexStore, .swiftpm as a path component, at any depth (root-level and nested cases)
  - Test: should_ignore_path returns True for typical real layouts (a .build/x86_64-apple-macosx/debug path, a DerivedData/Build/Products path, a .scip-cache/index-db path)
  - Test: should_ignore_path returns False for a Swift source file (/repo/Sources/App/main.swift) and for an .xcodeproj edit (/repo/App.xcodeproj/project.pbxproj) — over-broad ignoring is the failure mode this guards
  - Test: pre-existing ignores (.git, node_modules, .venv, __pycache__, dist, build) still return True (no regressions in the set)
  </behavior>
  <action>
  RED first: extend test_should_ignore_path_skips_vendor_and_git_dirs (or add a sibling test_swift_artifacts_are_ignored_wherever_they_appear plus a test_real_swift_layouts_are_ignored) per the behavior block, including the two False guards. Commit test(02-02).

  GREEN in src/jarvis/watch.py: extend _IGNORED_PATH_PARTS with exactly .scip-cache, .build, DerivedData, .index-store, IndexStore, .swiftpm — a pure set extension, no matcher change (D-07: single ignore path for all languages). Add a why-comment: the scip-swift cache now lives under the jarvis data dir (02-01), but xcodebuild and SwiftPM can still drop build products inside the working tree; these names are matched as components wherever they appear (D-08). Note in the comment that the one-time xcshareddata/swiftpm/configuration write xcodebuild makes on first index is deliberately NOT ignored — it is never rewritten afterward, so it costs one debounced reindex once, and ignoring it would start down the path of ignoring .xcodeproj internals where project.pbxproj edits are legitimate triggers. Commit feat(02-02).
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_watch.py -q</automated>
  </verify>
  <done>All six new names ignored at any depth; Swift sources and pbxproj edits still trigger; the existing ignore set unchanged</done>
  <acceptance_criteria>
  - src/jarvis/watch.py _IGNORED_PATH_PARTS contains all six additions and should_ignore_path itself is byte-identical in logic (set contents changed only)
  - tests/test_watch.py asserts the True cases for each new name and the False cases for Sources/*.swift and *.xcodeproj/project.pbxproj
  - No suffix-based matcher was added (D-08's list is directory names; the components-only matcher stays)
  - TDD gate: test(02-02) commit precedes feat(02-02)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: jarvis forget sweeps the repo's scip-swift cache directory</name>
  <files>src/jarvis/index_cli.py, tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:1096-1117 (_cmd_forget: slug resolution, registry.forget, index_dir rmtree, _remove_zoekt_shards, lancedb rmtree with ignore_errors=True — the exact spot the new sweep follows)
  - src/jarvis/config.py swift_cache_dir (landed in 02-01 — the path being swept)
  - tests/test_index_cli.py (the existing _cmd_forget tests — mirror their registry monkeypatching / JARVIS_DATA_DIR tmp-dir harness)
  - .planning/phases/02-scip-swift-toolchain-update/02-PATTERNS.md — forget sweep pattern (one rmtree line, ignore_errors=True because the cache may not exist for never-Swift-indexed repos)
  </read_first>
  <behavior>
  - Test: forgetting a slug removes <data_dir>/cache/scip-swift/<slug>/ (pre-created with a sentinel file) while a sibling slug's cache directory survives untouched
  - Test: forgetting a slug whose cache directory does not exist still exits 0 and prints the forgot line (never-Swift repo, or cache never created)
  </behavior>
  <action>
  RED first: add the two tests per the behavior block, reusing the existing forget-test harness (tmp JARVIS_DATA_DIR, registry fakes as the current tests do; create the cache dir via config.swift_cache_dir with a sentinel file). Commit test(02-02).

  GREEN in src/jarvis/index_cli.py: add exactly one line to _cmd_forget after the lancedb sweep — shutil.rmtree(config.swift_cache_dir(slug), ignore_errors=True) — with a brief why-comment (D-06: forgetting a repo removes everything jarvis stored for it; ignore_errors because the dir legitimately may not exist). Nothing else changes. Commit feat(02-02).
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_index_cli.py -m "not integration" -q</automated>
  </verify>
  <done>Forget removes the slug's cache dir, spares sibling slugs, and succeeds when the dir is absent</done>
  <acceptance_criteria>
  - _cmd_forget contains the swift_cache_dir rmtree with ignore_errors=True, positioned with the other cleanup sweeps
  - The sibling-slug survival assertion exists (proves the sweep cannot over-delete)
  - TDD gate: test(02-02) commit precedes feat(02-02)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| subprocess → index_cli.py | scip-swift --version output parsed at index time (locally installed binary; untrusted-format string, trusted provenance after 02-01's digest gate) |
| filesystem → watch.py | file-event paths from the watched repo (build tools can create arbitrary-named artifacts in-tree) |
| filesystem → _cmd_forget | recursive deletion scoped under the jarvis data dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | DoS (staleness) | should_ignore_path over-broad matching | medium | mitigate | Negative tests pin Sources/*.swift and *.xcodeproj/project.pbxproj as always-triggering; prohibition forbids wholesale .xcodeproj ignoring; the set extension is name-exact, not pattern-based |
| T-02-08 | Elevation | _cmd_forget rmtree blast radius | medium | mitigate | Sweep path constructed only as data_dir()/cache/scip-swift/<slug>; sibling-slug survival test; ignore_errors=True keeps forget non-fatal on absent dirs |
| T-02-09 | DoS | floor gate false-positives blocking legitimate indexing | medium | mitigate | Warn-by-omission on unparseable output (test-pinned), reuse of the proven parse_scip_version regex that tolerates the v-less format, Swift-only gating so other languages are structurally unaffected |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry packages in this plan — pure stdlib changes over existing modules; supply-chain posture inherited from 02-01's digest-verified binary |
</threat_model>

<verification>
- uv run python -m pytest tests/test_index_cli.py tests/test_watch.py -m "not integration" -q — all green
- uv run python -m pytest -m "not integration" -q -rs — full unit CI gate stays green (no regressions elsewhere)
</verification>

<success_criteria>
- D-04: too-old scip-swift fails loudly at Swift index time with a recovery hint; non-Swift paths untouched
- D-06: forget removes the repo's cache dir safely and idempotently
- D-07/D-08: watch drops the six Swift artifact directory names wherever they appear; legitimate triggers survive
</success_criteria>

## Artifacts this phase produces (Plan 02-02)

- src/jarvis/index_cli.py — constants `MIN_SCIP_SWIFT_VERSION = (0, 3, 0)`; functions `_scip_swift_version_output()` and `check_scip_swift_version()`; the Swift-gated call inside index_repo's swift branch; the `shutil.rmtree(config.swift_cache_dir(slug), ignore_errors=True)` sweep in _cmd_forget
- src/jarvis/watch.py — `_IGNORED_PATH_PARTS` gains `.scip-cache`, `.build`, `DerivedData`, `.index-store`, `IndexStore`, `.swiftpm` (no new env vars, CLI flags, or matcher logic)
- tests/test_index_cli.py — floor-gate suite (reject/accept/tolerate/missing-binary/non-swift-gate) + forget-sweep tests; tests/test_watch.py — Swift artifact ignore cases + non-ignore guards

## Flagged Assumptions (specless probe — unresolved edges, never auto-resolved)

| Requirement | Probe state | Flagged assumption made while planning |
|-------------|-------------|----------------------------------------|
| SWFT-03 (watch half) | unclassified / unresolved | "watch never self-triggers" is verified at unit level (should_ignore_path truth table) plus the accepted-and-documented one-shot xcshareddata event (orchestrator resolution #2). No automated live watch-loop-over-Swift-repo test exists in this phase — a real `jarvis watch` session on a Swift repo is manual-verification territory for /gsd-verify-work. |

<output>
Create `.planning/phases/02-scip-swift-toolchain-update/02-02-SUMMARY.md` when done
</output>
