---
phase: 02-scip-swift-toolchain-update
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.sh
  - tests/test_setup_sh.py
  - src/jarvis/config.py
  - tests/test_config.py
  - src/jarvis/index_cli.py
  - tests/test_index_cli.py
autonomous: true
requirements: [SWFT-01, SWFT-02, SWFT-03]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On darwin/arm64, `sh setup.sh --only scip-swift` resolves the latest scip-swift release from the GitHub Releases API, accepts it only at version >= 0.3.0, and installs a sha256-verified binary that runs (D-01, D-02)"
    - "A resolved latest below 0.3.0 fails the install loudly with a message naming the floor and stating no good release exists yet (D-02)"
    - "A tampered or malformed digest fails the install with a checksum-mismatch error before anything is installed (SWFT-02)"
    - "A tag failing the v<digits>.<digits>.<digits> shape check is rejected before any shell use of its value (SWFT-02, security)"
    - "An installed scip-swift older than the resolved latest is upgraded on re-run, never silently skipped; an install already at the resolved latest is skipped (version-aware skip — auto-roll cannot strand v0.1.2)"
    - "Non-macOS/non-arm64 hosts skip the scip-swift install with exit 0 and make no API request (platform gate stays first)"
    - "Every Swift index invocation passes --cache-dir <data_dir>/cache/scip-swift/<slug>/ on both the swiftpm and xcodebuild paths (D-05)"
    - "jarvis never creates the cache directory itself — it only computes the path (upstream scip-swift creates it)"
    - "A real end-to-end run — fresh bin dir install, then jarvis index on a git-init'd copy of an .xcodeproj fixture — publishes an index with language swift and leaves the cache under JARVIS_DATA_DIR/cache/scip-swift/<slug>/, with no indexer cache/build artifacts in the repo tree"
  artifacts:
    - path: "setup.sh"
      provides: "latest-resolution + floor + digest-verified scip-swift install"
      contains: "SCIP_SWIFT_MIN_VERSION"
    - path: "src/jarvis/config.py"
      provides: "swift_cache_dir(slug, root) path helper"
      contains: "swift_cache_dir"
    - path: "src/jarvis/index_cli.py"
      provides: "--cache-dir injection on every Swift indexer invocation"
      contains: "swift_cache_dir"
    - path: "tests/test_setup_sh.py"
      provides: "resolution/floor/digest/version-aware-skip tests over the SCIP_SWIFT_API_URL seam"
  key_links:
    - from: "setup.sh install_scip_swift"
      to: "api.github.com /releases/latest"
      via: "download_to on the SCIP_SWIFT_API_URL-overridable URL; sed/grep extraction of tag_name, digest, browser_download_url"
      pattern: "SCIP_SWIFT_API_URL"
    - from: "src/jarvis/index_cli.py index_repo (swift branch)"
      to: "src/jarvis/config.py swift_cache_dir"
      via: "_swift_indexer_cmd(indexer_cmd, repo_path, scheme, config.swift_cache_dir(slug)) appends --cache-dir"
      pattern: "--cache-dir"
  prohibitions:
    - "The installer must not widen the darwin/arm64 platform gate as a side effect of the v0.3.0 universal binary — x86_64 support stays out of scope (RESEARCH assumption A4; not in D-01..D-10)"
    - "setup.sh must not grow a second checksum path — no .sha256 sidecar probing alongside the API digest; one checksum source, one code path (orchestrator resolution #1)"
    - "The scip-swift install skip must not be presence-gated — an installed binary older than the resolved latest is always upgraded, never skipped (RESEARCH Pitfall 2)"

assumption_delta_scan: "detected (term: fallback, ROADMAP Phase-2 contingency prose)"
---

<objective>
Tracer plan for Phase 2: move the scip-swift toolchain off the broken v0.1.2 pin onto latest-at-install resolution with a 0.3.0 floor and API-digest verification, and plumb the per-repo out-of-repo cache directory into every Swift indexer invocation.

Purpose: v0.2.0/v0.2.1 carry an xcodebuild-dispatch regression and v0.1.2 predates the incremental cache; upstream v0.3.0 (Latest, 2026-08-18) fixes dispatch and honors --cache-dir. This plan proves the two riskiest contracts end-to-end — POSIX-sh API resolution with untrusted-input validation, and the cache-dir argv contract — before the expansion plans add runtime guards and CI verification.

Output: rewritten install_scip_swift in setup.sh (+ version_ge and install_tarball_binary_with_digest helpers, SCIP_SWIFT_API_URL test seam), swift_cache_dir in config.py, --cache-dir in _swift_indexer_cmd, and the test suites that pin all of it.

D-03 note (locked resolution): cutting an upstream release is NOT needed — v0.3.0 already carries the dispatch fix (verified end-to-end in research). No tag, no asset upload, no release.yml patch happens in this phase.
</objective>

<execution_context>
@/Users/ddphuong/.claude/gsd-core/workflows/execute-plan.md
@/Users/ddphuong/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-scip-swift-toolchain-update/02-CONTEXT.md
@.planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md
@.planning/phases/02-scip-swift-toolchain-update/02-PATTERNS.md
@setup.sh
@src/jarvis/index_cli.py
@src/jarvis/config.py
</context>

<assumption_delta_decision>
**Signal:** assumption-delta scan detected the term "fallback" (pluralization) in the phase scope — from ROADMAP's Phase-2 contingency prose referencing Phase 3's fallback work, and RESEARCH's "redirect fallback" alternative for tag resolution.

**Decision: ADD-ALONGSIDE.** The existing sidecar-verified helper `install_tarball_binary` (setup.sh:258-304) stays untouched for zoekt/scip; the digest route lands as a sibling helper `install_tarball_binary_with_digest`. No promotion of the sidecar path to digest-only (zoekt/scip installs keep their published-sidecar contract), and no dual checksum source inside scip-swift's own path (digest-only per orchestrator resolution #1). Within scip-swift resolution there is explicitly NO redirect fallback path — one API call serves tag + asset URL + digest, and failure is loud (rate-limit posture, RESEARCH Pitfall 4). This mirrors the Phase-1 add-alongside precedent for taxonomy extension.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: install_scip_swift resolves latest at install time with a 0.3.0 floor and API-digest verification</name>
  <precondition>Network access to api.github.com and the github.com release CDN (anonymous; both verified reachable from this host in research, 2026-08-22)</precondition>
  <files>setup.sh, tests/test_setup_sh.py</files>
  <read_first>
  - setup.sh:39-63 (pin block: SCIP_SWIFT_VERSION at line 48, SCIP_SWIFT_REPO at line 53 with its why-comment)
  - setup.sh:219-304 (download_to, verify_sha256, sha256_of, install_tarball_binary with its sidecar download + trap/EXIT idiom)
  - setup.sh:430-446 (installed_scip_matches_pin — the version-aware skip pattern to mirror, including its bin_dir-first/PATH-fallback resolution order)
  - setup.sh:496-540 (install_zoekt — ZOEKT_BASE_URL seam at 505-507 and platform-gate-first ordering)
  - setup.sh:542-575 (scip_swift_asset_name + current install_scip_swift body: darwin/arm64 gate 555-558, presence-gated skip 560-563, sidecar install call 569)
  - tests/test_setup_sh.py:35-48 (run_func harness with JARVIS_SETUP_SOURCED/JARVIS_BIN_DIR env)
  - tests/test_setup_sh.py:718-748 (test_install_zoekt_extracts_both_binaries — the file:// seam test pattern to mirror)
  - tests/test_setup_sh.py:749-819 (the scip-swift test section to rewrite: org-pin test 774 stays; asset-name test 786, pin-floor test 793, presence-skip test 806 are replaced)
  - .planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md — Patterns 1 (extraction snippets validated against the live API), Pitfalls 2-6, Code Examples (version_ge verified under sh+dash)
  </read_first>
  <behavior>
  - Test: version_ge compares numerically, not lexicographically — `version_ge 0.10.0 0.3.0` exits 0; `version_ge v0.3.0 0.2.1` exits 0; `version_ge 0.2.2 0.3.0` exits non-zero; `version_ge 0.3.0 0.3.0` exits 0 (equal is GE)
  - Test: install_scip_swift darwin arm64 with seam-served JSON (tag v0.3.0, digest matching a locally built tarball, file:// browser_download_url) installs an executable scip-swift into JARVIS_BIN_DIR
  - Test: seam-served JSON with tag v0.2.1 exits non-zero and the combined output names 0.3.0 and states no good release exists yet (floor failure is loud)
  - Test: seam-served JSON whose digest does not match the served tarball exits non-zero with a checksum-mismatch error and installs nothing
  - Test: seam-served JSON whose tag carries shell metacharacters (e.g. a tag value with a semicolon sequence) is rejected by shape validation — non-zero exit, and no side-effect file named by the injected payload exists afterward
  - Test: a PATH-stub scip-swift whose --version prints 0.3.0 (…) alongside JSON resolving v0.3.0 is skipped ("already installed" family message); the same setup with the stub printing 0.1.2 (…) proceeds to install from the seam
  - Test: install_scip_swift linux amd64 with SCIP_SWIFT_API_URL pointing at a nonexistent file:// path still exits 0 with the skip message — proving the platform gate runs before any fetch
  - Test (kept, unchanged): test_scip_swift_repo_is_the_org_not_the_personal_owner, test_install_scip_swift_skips_on_intel_mac, test_install_scip_swift_skips_on_linux_without_failing
  </behavior>
  <action>
  RED first: rewrite the scip-swift section of tests/test_setup_sh.py per the behavior block. Build a GitHub-shaped releases/latest JSON fixture per test (tag_name, assets[] with name, digest "sha256:<hex>", browser_download_url pointing at a file:// tarball staged in tmp_path containing one member named scip-swift — a stub script), write it to tmp_path, and pass it via run_func env SCIP_SWIFT_API_URL="file://<path>" exactly like the ZOEKT_BASE_URL pattern. Delete test_scip_swift_asset_name_uses_macos_not_darwin and test_scip_swift_pin_is_at_least_v0_1_2 (the concepts they guarded — asset naming and version floor — are now guarded by the resolution/floor tests) and replace test_install_scip_swift_skips_when_present with the two version-aware skip variants (stub --version output becomes the version source). Commit test(02-01).

  GREEN in setup.sh (strictly POSIX sh, dash-safe, no jq, comments document why):
  - Replace the SCIP_SWIFT_VERSION="v0.1.2" pin at line 48 with SCIP_SWIFT_MIN_VERSION="0.3.0" and rewrite the surrounding rationale comment: v0.2.0/v0.2.1 silently ignore --build-tool xcodebuild (dispatch regression), the fix landed in 0.3.0 (upstream commit 9bcf1688), so the floor is inclusive-ge on 0.3.0 — a hypothetical 0.2.2 would still be broken. Keep SCIP_SWIFT_REPO unchanged (org-pin test guards it).
  - Add a version_ge() helper (numeric 3-field IFS/cut compare, tr-stripping any v prefix, empty fields defaulting to 0 — no sort -V, which is GNU-only) beside the other helpers, with the research-verified comment noting 0.10.0 > 0.3.0 must compare numerically.
  - Add install_tarball_binary_with_digest(_tar_url, _expected_hex, _member, _dest_name): same body as install_tarball_binary minus the sidecar download + cut block — the caller supplies the expected hash directly; reuse download_to, verify_sha256, the mktemp/trap EXIT cleanup idiom, the single-member tar -xzf extraction, ensure_bin_dir, mv, chmod. Leave install_tarball_binary itself untouched (assumption-delta: add-alongside).
  - Rewrite install_scip_swift, keeping statement order: (1) darwin/arm64 skip gate FIRST, unchanged; (2) fetch the API JSON to a mktemp file via download_to on _api="${SCIP_SWIFT_API_URL:-https://api.github.com/repos/${SCIP_SWIFT_REPO}/releases/latest}" (new env seam, ZOEKT_BASE_URL pattern, with the same overridable-for-tests why-comment); (3) extract _tag, _digest, _url with the research-validated line-oriented sed/grep pipeline (tag_name sed substitution; first "digest" grep with the sha256: prefix kept then stripped; first browser_download_url ending in .tar.gz), failing loudly if any field is empty; (4) shape-validate before use: tag must match the v<digits>.<digits>.<digits> family via a case pattern, digest must be sha256: followed by exactly 64 lowercase hex (case pattern), URL must start with https:// or file:// (file:// exists solely as the test seam — say so in the comment); (5) floor: version_ge "$_tag" "$SCIP_SWIFT_MIN_VERSION" || fail loudly naming the resolved tag, the floor, and that no good release exists yet; (6) version-aware skip: resolve the installed version (executable check in bin_dir first, else have_cmd on PATH — the installed_scip_matches_pin resolution order), take the FIRST whitespace-delimited token of its --version output, and skip only when version_ge "$_installed" "$_tag" (still honoring FORCE=1 bypass); (7) install_tarball_binary_with_digest "$_url" "$_digest_hex" scip-swift scip-swift — the tar member stays the literal scip-swift, never a JSON-derived value; (8) on failure, the existing build-from-source error line stays. Every expansion double-quoted; no eval anywhere.
  - Delete scip_swift_asset_name (no callers remain — the URL comes from the API; constructing it from the tag is the anti-pattern RESEARCH flags because the naming convention already changed once).
  Commit feat(02-01).
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_setup_sh.py -q && sh -n setup.sh</automated>
  </verify>
  <done>Resolution/floor/digest/shape/skip tests all pass under the project POSIX shell; setup.sh parses under sh -n; no trace of the old pin variable or asset-name helper remains</done>
  <acceptance_criteria>
  - grep -c 'SCIP_SWIFT_MIN_VERSION="0.3.0"' setup.sh (via sh -c on the file) is exactly 1, and the old SCIP_SWIFT_VERSION assignment no longer exists in setup.sh
  - setup.sh contains the seam default _api assignment keyed on SCIP_SWIFT_API_URL with the https://api.github.com default
  - The darwin/arm64 gate precedes the API fetch in install_scip_swift (verified by the nonexistent-seam linux test passing)
  - tests/test_setup_sh.py contains passing tests for: numeric version_ge, resolve+install, below-floor loud failure, digest mismatch, malformed-tag rejection, skip-when-current, reinstall-when-outdated, linux no-fetch skip
  - test_scip_swift_repo_is_the_org_not_the_personal_owner still present and passing
  - TDD gate: a test(02-01) commit precedes the feat(02-01) commit
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Per-repo swift cache dir plumbed as --cache-dir on every Swift indexer invocation, proven end-to-end</name>
  <precondition>scip, zoekt-git-index, and xcodebuild runnable on this host (full jarvis Swift index pipeline; verified 2026-08-22), and Task 1's real scip-swift v0.3.0 installed into a temp bin dir or on PATH</precondition>
  <files>src/jarvis/config.py, src/jarvis/index_cli.py, tests/test_config.py, tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/config.py:28-74 (data_dir / lancedb_dir / index_dir helpers — the data_dir(root) delegation shape to mirror)
  - src/jarvis/index_cli.py:193-214 (_swift_indexer_cmd current signature and argv construction) and its call site near line 793-797 inside index_repo (the language == "swift" branch where the slug is already resolved)
  - tests/test_index_cli.py:229-280 (the _swift_indexer_cmd argv tests to extend — tmp_path fixtures, Package.swift / .xcodeproj setup)
  - tests/test_config.py (existing data-dir path-helper tests — mirror their JARVIS_DATA_DIR / root= conventions)
  - .planning/phases/02-scip-swift-toolchain-update/02-RESEARCH.md — Pattern 3 (cache safety: upstream manifest auto-invalidates; jarvis never creates the dir) and Pitfall 1 (the .scip-cache CLI-help vs code divergence)
  </read_first>
  <behavior>
  - Test: config.swift_cache_dir(slug) with JARVIS_DATA_DIR pointed at a tmp dir returns <tmp>/cache/scip-swift/<slug>, and the root= parameter overrides the base exactly like lancedb_dir
  - Test: _swift_indexer_cmd with an .xcodeproj present returns base + --build-tool xcodebuild + (--scheme <name> when given) + --cache-dir <path>, in that order
  - Test: _swift_indexer_cmd with only Package.swift (swiftpm path) returns base + --cache-dir <path> — the cache flag is NOT xcodebuild-only
  - Test: the cache path string in the returned argv equals str(config.swift_cache_dir(slug)) for the slug under test
  </behavior>
  <action>
  RED first: add the config test and extend/replace the _swift_indexer_cmd argv tests per the behavior block (the function gains a fourth parameter, so every existing call site test updates to the new signature). Commit test(02-01).

  GREEN: add swift_cache_dir(slug: str, root: Path | None = None) -> Path to config.py returning data_dir(root) / "cache" / "scip-swift" / slug, with a why-docstring naming D-05 (per-repo isolation, no cross-repo IndexStore interference; jarvis computes the path only — upstream scip-swift creates the directory and its manifest handles invalidation on binary/toolchain change). Change _swift_indexer_cmd to (base_cmd: list[str], repo_path: Path, scheme: str | None, cache_dir: Path) -> list[str]: the early swiftpm return becomes base_cmd plus --cache-dir str(cache_dir); the xcodebuild branch appends --build-tool xcodebuild, the optional --scheme pair, then --cache-dir str(cache_dir) — the flag rides every Swift invocation. Update the index_repo call site to pass config.swift_cache_dir(slug). Add a short why-comment at the call site: cache outside repo trees keeps build products out of the working copy and out of ~/Library/Developer/Xcode/DerivedData for the build itself. Commit feat(02-01).

  Then run the end-to-end proof (tracer verification, no files committed from it): with JARVIS_BIN_DIR and JARVIS_DATA_DIR both pointed at fresh mktemp dirs, sh setup.sh --only scip-swift and confirm the installed binary's --version prints 0.3.0 (…); clone jarvis-intelligence/scip-swift at tag v0.3.0 (or reuse a local clone), copy its Fixtures/XcodeTestProject into a fresh tmp dir, git init + git add + git commit there (language detection and zoekt read git), then run jarvis index on it with JARVIS_DATA_DIR still pointed at the temp dir. Assert: exit 0 and status indexed / language swift; the cache landed under <tmp-data>/cache/scip-swift/<slug>/ containing manifest.json and derived-data; the repo tree contains no .scip-cache and no .build (the one-shot xcshareddata/swiftpm/configuration file inside the .xcworkspace is expected and accepted — orchestrator resolution #2). Clean up all temp dirs. Record the evidence in the SUMMARY.
  </action>
  <verify>
    <automated>uv run python -m pytest tests/test_config.py tests/test_index_cli.py -m "not integration" -q</automated>
  </verify>
  <done>Unit tests pass for both build paths carrying --cache-dir and for the config helper; the end-to-end run installed v0.3.0 via the new resolution, indexed a real .xcodeproj repo to status indexed, kept the cache entirely under the JARVIS_DATA_DIR cache root, and left the repo tree free of cache/build artifacts</done>
  <acceptance_criteria>
  - src/jarvis/config.py defines swift_cache_dir delegating to data_dir(root) / "cache" / "scip-swift" / slug (one line of path arithmetic, mirroring lancedb_dir)
  - src/jarvis/index_cli.py contains exactly one _swift_indexer_cmd definition with a cache_dir parameter, and its two return paths both append --cache-dir
  - The index_repo call site passes config.swift_cache_dir(slug); no other callers of _swift_indexer_cmd exist
  - jarvis source contains no directory-creation call for the swift cache path (upstream owns creation — anti-pattern per research)
  - TDD gate: a test(02-01) commit precedes this task's feat(02-01) commit
  - SUMMARY records the e2e evidence: installed version output, index status line, cache-dir listing, repo-tree cleanliness check
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → setup.sh (API JSON) | Untrusted api.github.com response parsed by POSIX sh; tag/digest/URL flow into shell variables, a curl URL, and version comparisons |
| CDN → setup.sh (tarball) | Release asset bytes verified against the server-side digest before install |
| shell environment → setup.sh | JARVIS_BIN_DIR / FORCE / SCIP_SWIFT_API_URL test seams (trusted local env, ZOEKT_BASE_URL precedent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering/Elevation | install_scip_swift JSON extraction | high | mitigate | Shape-validate tag (v<digits>.<digits>.<digits> case pattern) and digest (sha256: + 64 lowercase hex) before any use; browser_download_url restricted to https:// (file:// only as the documented test seam); every expansion double-quoted; tar member is the literal scip-swift, never JSON-derived; no eval — enforced by the malformed-tag rejection test |
| T-02-02 | Tampering (supply chain) | swapped/substituted release asset | high | mitigate | verify_sha256 of the downloaded tarball against the immutable server-computed API digest before extraction; tampered-digest unit test fails the install |
| T-02-03 | Tampering | tar archive path traversal | medium | mitigate | Extract exactly one pinned member (tar -xzf … scip-swift), never a wholesale extract; upstream tarball verified single-member in research |
| T-02-04 | Spoofing | typosquatted upstream repo | low | mitigate | SCIP_SWIFT_REPO org-pin stays guarded by test_scip_swift_repo_is_the_org_not_the_personal_owner (kept unchanged) |
| T-02-05 | DoS | anonymous API rate limit (60 req/hr/IP) exhausting in CI | low | accept | Exactly one API call per install (resolution + digest in one fetch); failures surface the HTTP status loudly — a redirect-only fallback cannot verify checksums, so loud failure is the chosen posture |
| T-02-06 | DoS | broken release auto-rolled to every install | medium | mitigate | Inclusive floor >= 0.3.0 (D-02) + version-aware reinstall guard + setup-smoke CI exercising the real install on macOS |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | No registry packages installed this phase; the sole external artifact is the org-owned scip-swift release tarball, provenance established by digest match + end-to-end run (Package Legitimacy Audit, verdict OK) — no [ASSUMED]/[SUS] packages, no blocking checkpoint required |
</threat_model>

<verification>
- uv run python -m pytest tests/test_setup_sh.py tests/test_config.py tests/test_index_cli.py -m "not integration" -q — all green
- sh -n setup.sh — POSIX parse gate
- Live install smoke: JARVIS_BIN_DIR=$(mktemp -d) sh setup.sh --only scip-swift, then the installed binary's --version reports 0.3.0 or newer
- Post-push: setup-smoke CI (macos-latest leg) runs the rewritten installer against the real API — the external proof that resolution + floor + digest work end-to-end on a clean runner
</verification>

<success_criteria>
- SWFT-01 (install side): the pin is off v0.1.2 — installs resolve latest with a 0.3.0 floor
- SWFT-02: asset name/URL/digest all come from the API (current naming handled); checksum verification retained via the immutable digest; tamper/malformed inputs fail closed
- SWFT-03 (cache side): --cache-dir rides every Swift invocation to a per-slug directory under the jarvis data dir; e2e proof shows nothing cache-related in the repo tree
</success_criteria>

## Artifacts this phase produces (Plan 02-01)

- setup.sh — env var `SCIP_SWIFT_API_URL` (optional test seam, default https://api.github.com/repos/jarvis-intelligence/scip-swift/releases/latest); constant `SCIP_SWIFT_MIN_VERSION="0.3.0"`; shell functions `version_ge()` and `install_tarball_binary_with_digest()`; rewritten `install_scip_swift()`; REMOVED: `SCIP_SWIFT_VERSION` pin and `scip_swift_asset_name()`
- src/jarvis/config.py — `swift_cache_dir(slug: str, root: Path | None = None) -> Path`
- src/jarvis/index_cli.py — `_swift_indexer_cmd` gains the `cache_dir: Path` parameter; every Swift invocation appends `--cache-dir`
- tests/test_setup_sh.py — replaced scip-swift test section (resolution/floor/digest/shape/version-aware-skip/no-fetch-skip); tests/test_config.py and tests/test_index_cli.py — new cache-path and argv coverage

## Flagged Assumptions (specless probe — unresolved edges, never auto-resolved)

| Requirement | Probe state | Flagged assumption made while planning |
|-------------|-------------|----------------------------------------|
| SWFT-02 | unclassified / unresolved | "checksum verification retained" is satisfied by the GitHub API asset `digest` field (immutable, official since 2025-06-03), NOT by a published .sha256 sidecar — this intentionally deviates from ROADMAP SC1's "published sidecar" wording per orchestrator resolution #1. Verifier should confirm the digest-verify tests exist rather than expecting sidecar logic. |
| SWFT-01 (shared with 02-03) | unclassified / unresolved | "verified against the pinned release" means: live install smoke + CI macOS dispatch smoke (Plan 02-03). RESEARCH assumption A1 (macos-latest runners are arm64 with Xcode) is NOT exercised live in this repo yet — first real proof lands when setup-smoke runs. |

<output>
Create `.planning/phases/02-scip-swift-toolchain-update/02-01-SUMMARY.md` when done
</output>
