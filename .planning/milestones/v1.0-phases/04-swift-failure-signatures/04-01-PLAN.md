---
phase: 04-swift-failure-signatures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jarvis/index_cli.py
  - tests/test_index_cli.py
autonomous: true
requirements: [SWFT-04]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "SC1-A: a Swift repo failing with the captured no-build-system stderr (Could not detect a build system / no Package.swift and no .xcodeproj/.xcworkspace found) degrades to search-only automatically with no opt-in, and the registry row reports status search-only, search_only true, origin signature, status_reason equal to the matched entry reason verbatim"
    - "SC1-B: a Swift repo failing with the captured no-IndexStore stderr (Build succeeded but no IndexStore was produced) degrades identically — same branch, same origin, reason verbatim"
    - "SC2: both Swift entries embed only path-free tokens quoted verbatim from the real scip-swift 0.3.0 stderr (captured 2026-08-23), and each is pinned by a unit test embedding the exact captured stderr with a provenance comment naming binary version and trigger repo shape"
    - "SC3 + empty probe: a Swift failure matching no signature still fails hard — generic swift-build and xcodebuild wrapper carriers, empty or whitespace-only stderr, and stdout-only carriers all leave _search_only_reason returning None, so index_repo raises, exits non-zero, and publishes nothing"
    - "Adjacency probe: the signature path and the opt-in degrade path stay separate — a matched signature takes the automatic origin-signature permanent search-only branch regardless of the phase-3 fallback flag; the degraded state remains for unclassified failures only (pinned by the existing preempt test, unchanged)"
    - "Ordering probe: when a carrier matches multiple entries the first matching entry in _SEARCH_ONLY_SIGNATURES list order wins; the two Swift entries are mutually exclusive so Swift classification is never ambiguous; the Kotlin/AGP entries and the matcher stay byte-identical"
    - "Every new test runs binary-free — _run mocked, check_scip_version and check_scip_swift_version monkeypatched — and passes on the binary-free ubuntu CI unit legs"
  artifacts:
    - path: "src/jarvis/index_cli.py"
      provides: "two appended Swift entries in _SEARCH_ONLY_SIGNATURES plus a capture-provenance drift comment (append-only; matcher and existing entries untouched)"
      contains: "Could not detect a build system"
    - path: "tests/test_index_cli.py"
      provides: "five pinning and negative tests covering the Swift signature set, empty carriers, and first-match order"
      contains: "test_swift_no_build_system_signature_degrades_search_only"
  key_links:
    - from: "src/jarvis/index_cli.py (_SEARCH_ONLY_SIGNATURES Swift entries)"
      to: "src/jarvis/index_cli.py (_search_only_reason loop — unchanged)"
      via: "all(token in output for token in required) substring scan over the failure carrier raised by _run"
      pattern: "_search_only_reason\\(str\\(exc\\)\\)"
    - from: "src/jarvis/index_cli.py (signature consult branch, unchanged)"
      to: "src/jarvis/registry.py (ORIGIN_SIGNATURE upsert)"
      via: "registry.upsert(..., search_only=True, status_origin=ORIGIN_SIGNATURE, status_reason=reason) on the existing path"
      pattern: "status_origin=ORIGIN_SIGNATURE"
    - from: "tests/test_index_cli.py (pinning tests)"
      to: "src/jarvis/index_cli.py (_SEARCH_ONLY_SIGNATURES constant)"
      via: "tests look the reason up by the exact token tuple, so drift in tokens or reason text fails loudly"
      pattern: "tokens == \\(\"Could not detect a build system\""
    - from: "tests/test_index_cli.py (all new Swift tests)"
      to: "src/jarvis/index_cli.py (check_scip_swift_version pre-pipeline gate)"
      via: "monkeypatch to a lambda returning None keeps the tests hermetic on binary-free CI legs"
      pattern: "check_scip_swift_version\", lambda: None"
  prohibitions:
    - requirement_id: SWFT-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT match the generic build-failure wrappers ('swift build' failed with exit code 1: and 'xcodebuild' failed with exit code 65:) — every fixable Swift failure keeps failing hard"
    - requirement_id: SWFT-04
      category: transparency
      status: resolved
      verification: test
      statement: "MUST NOT put remedy prose in reason text — reasons are causes only (phase-1 D-11); recovery stays the read-time per-origin mapping"
    - requirement_id: SWFT-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT use regex matching — plain substring tokens only (locked CONTEXT decision, Kotlin/AGP parity)"
    - requirement_id: SWFT-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT modify the Kotlin/AGP entries, the _search_only_reason matcher, or the phase-3 degrade gate — the production change is append-only to the constant"
    - requirement_id: SWFT-04
      category: safety
      status: resolved
      verification: test
      statement: "MUST NOT require the scip-swift binary in unit tests — no subprocess spawn of scip-swift; CI ubuntu legs are binary-free"

---

<objective>
Add the two verified scip-swift failure signatures — captured verbatim from the pinned scip-swift 0.3.0 binary's real stderr — to `_SEARCH_ONLY_SIGNATURES`, so known-unfixable Swift failures degrade to search-only automatically (no opt-in, Kotlin/AGP parity), and pin them with unit tests embedding the exact captured output. Unmatched Swift failures keep failing hard.

Purpose: closes SWFT-04 — the last Swift-toolchain requirement. Phase 1 built the generic signature mechanism and phase 3 built the opt-in degrade ladder precisely so this phase is data-only: two constant entries plus tests, zero wiring changes (04-RESEARCH.md: "phase 1 built the whole degradation-reporting pipeline around a generic signature mechanism precisely so that phases like this one are data-only").
Output: 2 appended entries in `src/jarvis/index_cli.py`, 5 new test functions in `tests/test_index_cli.py`.

User decisions implemented (04-CONTEXT.md, LOCKED):
- Area 1 — capture & set: verbatim stderr capture from the real binary, two SC1-named classes as the v1 set, exact-output pinning tests with provenance comments, substring matching identical to Kotlin/AGP.
- Area 2 — semantics & interactions: matched signature takes the automatic `origin='signature'` permanent search-only path regardless of the fallback flag; wording drift on future releases fails hard by design, documented in the constant block.
</objective>

## Artifacts this phase produces

Every symbol this phase creates (all others already exist and stay untouched):

| Artifact | Symbol | Shape |
|---|---|---|
| `src/jarvis/index_cli.py` | `_SEARCH_ONLY_SIGNATURES` entry 4 | tokens `("Could not detect a build system", "no Package.swift and no .xcodeproj/.xcworkspace found")` + reason (verbatim in Task 1) |
| `src/jarvis/index_cli.py` | `_SEARCH_ONLY_SIGNATURES` entry 5 | tokens `("Build succeeded but no IndexStore was produced",)` + reason (verbatim in Task 2) |
| `src/jarvis/index_cli.py` | capture-provenance comment block | precedes the Swift entries; names binary 0.3.0, capture date 2026-08-23, re-capture-on-bump, drift-fails-hard rule |
| `tests/test_index_cli.py` | `test_swift_no_build_system_signature_degrades_search_only` | end-to-end pin (mocked `_run`, real registry row assertions) |
| `tests/test_index_cli.py` | `test_swift_no_index_store_signature_degrades_search_only` | same shape, IndexStore carrier |
| `tests/test_index_cli.py` | `test_swift_generic_build_failure_wrapper_never_matches` | pure `_search_only_reason` negative pins (SC3) |
| `tests/test_index_cli.py` | `test_empty_or_stdout_only_failure_carriers_never_match` | pure `_search_only_reason` empty-carrier pins (empty probe) |
| `tests/test_index_cli.py` | `test_search_only_reason_first_listed_match_wins` | pure `_search_only_reason` ordering pin (ordering probe) |

No new modules, no new production functions, no schema changes, no new dependencies, no changes to `server.py` / `registry.py` / `watch.py` / `config.py`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-swift-failure-signatures/04-CONTEXT.md
@.planning/phases/04-swift-failure-signatures/04-RESEARCH.md
@.planning/phases/04-swift-failure-signatures/04-PATTERNS.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Signature-1 tracer — no-build-system carrier degrades search-only end-to-end</name>
  <files>tests/test_index_cli.py, src/jarvis/index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:98-132 — comment block, the three Kotlin/AGP entries in _SEARCH_ONLY_SIGNATURES, and the unchanged _search_only_reason matcher
  - src/jarvis/index_cli.py:1060-1084 — the unchanged signature consult branch (where the new entries are matched)
  - tests/test_index_cli.py:1896-1947 — test_signature_fallback_stamps_signature_origin_and_matched_reason, the exact analog to mirror
  - tests/test_index_cli.py:1335-1342 — the check_scip_swift_version monkeypatch pattern (Pitfall 3: CI unit legs have no scip-swift)
  - tests/test_index_cli.py:22-46 — SWIFT_FIXTURE_REPO, _fake_completed_process, _init_git_repo helpers
  - 04-RESEARCH.md "Code Examples" → "Captured Signature 1" — the verbatim stderr and the proposed entry
  </read_first>
  <behavior>
  - RED: with no matching entry in the constant, index_repo on the mocked carrier raises IndexingError out of the signature branch — the new test must fail exactly that way before any production edit.
  - GREEN: index_repo returns the slug; the Registry row asserts status == SEARCH_ONLY_STATUS, search_only is True, status_origin == ORIGIN_SIGNATURE, status_reason == the new entry's reason verbatim (looked up from _SEARCH_ONLY_SIGNATURES by the exact token tuple, so token or reason drift fails loudly with StopIteration).
  </behavior>
  <action>
  RED first. Add test_swift_no_build_system_signature_degrades_search_only to tests/test_index_cli.py directly after test_signature_fallback_stamps_signature_origin_and_matched_reason (keeps signature tests grouped). Mirror that Kotlin analog exactly (tmp_path/monkeypatch signature, shutil.copytree of the fixture, _init_git_repo, local imports of SEARCH_ONLY_STATUS/_SEARCH_ONLY_SIGNATURES/IndexingError/index_repo and ORIGIN_SIGNATURE, _fake_run raising on step.endswith(" index") and returning _fake_completed_process(cmd) otherwise so the zoekt publish path completes, monkeypatch of check_scip_version and _run_semantic_stage) with three Swift deltas:
  (1) fixture is SWIFT_FIXTURE_REPO, not FIXTURE_REPO — detection only needs tracked .swift files; the binary is never invoked;
  (2) one extra monkeypatch beside the existing check_scip_version one: monkeypatch.setattr("jarvis.index_cli.check_scip_swift_version", lambda: None) — the swift branch probes the binary pre-pipeline and would raise "scip-swift not found on PATH" before the mocked indexer step on binary-free machines;
  (3) the carrier is the exact captured stderr with only the repo path rewritten to the tmp repo — the single line: Error: Could not detect a build system at {repo_dir}: no Package.swift and no .xcodeproj/.xcworkspace found. Pass --build-tool swiftpm or --build-tool xcodebuild explicitly. Precede the carrier with a provenance comment: captured 2026-08-23 from scip-swift 0.3.0 (sha256 b0de7201…85a5, Xcode 26.3 / Swift 6.2.4) against a git repo with tracked .swift sources and no Package.swift and no .xcodeproj/.xcworkspace; carrier verified end-to-end through a real jarvis index run (04-RESEARCH.md, out/jarvis-nobuildsystem.txt).
  Look up swift_reason via next(reason for tokens, reason in _SEARCH_ONLY_SIGNATURES if tokens == ("Could not detect a build system", "no Package.swift and no .xcodeproj/.xcworkspace found")) — same tripwire idiom as the Kotlin test. Run the test; it MUST fail (IndexingError propagates, no search-only publish happens). Commit: test(04-01): add failing swift no-build-system pinning test.
  GREEN. In src/jarvis/index_cli.py, append to _SEARCH_ONLY_SIGNATURES after the AGP entry and before the closing paren: first a short comment block (why-comment, matching the file's style — do not quote any token text inside it, so the entry remains the only place the strings live) stating: captured 2026-08-23 from the pinned scip-swift 0.3.0 against known-failing repo shapes; tokens are deliberately path-free because both scip-swift error lines interpolate repo/cache paths; on a scip-swift version bump these must be re-captured — wording drift fails hard (unmatched → hard failure) and must never silently degrade. Then the entry with tokens exactly ("Could not detect a build system", "no Package.swift and no .xcodeproj/.xcworkspace found") and reason exactly: the repo has Swift sources but neither a Package.swift nor an Xcode project, so scip-swift has no build system to run — a cause, never a remedy (phase-1 D-11: reasons only, recovery stays read-time). Touch nothing else: the matcher, the consult branch, and the three Kotlin/AGP entries stay byte-identical (append-only, per prohibition). Run the test; it MUST pass. Commit: feat(04-01): add scip-swift no-build-system signature.
  </action>
  <verify>
  <automated>uv run pytest "tests/test_index_cli.py::test_swift_no_build_system_signature_degrades_search_only" -q</automated>
  </verify>
  <acceptance_criteria>
  - Source: _SEARCH_ONLY_SIGNATURES contains a fourth entry whose token tuple is exactly ("Could not detect a build system", "no Package.swift and no .xcodeproj/.xcworkspace found") with the reason string quoted verbatim in <action>, preceded by the provenance/drift comment block; the three Kotlin/AGP entries above it are unchanged.
  - Test: test_swift_no_build_system_signature_degrades_search_only asserts all four registry fields (status, search_only, status_origin, status_reason verbatim) and passes; it includes both the check_scip_version and check_scip_swift_version monkeypatches and the provenance comment.
  - TDD gates: the test(...) commit exists and its recorded run failed before the feat(...) commit landed.
  - Suite: uv run pytest tests/test_index_cli.py -m "not integration" -q is green at 187 passed (measured baseline 186 + 1 new), 12 deselected.
  </acceptance_criteria>
  <done>The no-build-system carrier degrades search-only end-to-end through index_repo with origin signature and the matched reason persisted verbatim; RED commit precedes GREEN commit; quick suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Signature-2 — no-IndexStore carrier degrades search-only</name>
  <files>tests/test_index_cli.py, src/jarvis/index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:98-132 — constant plus the Task-1 Swift entry and comment block just added
  - tests/test_index_cli.py — test_swift_no_build_system_signature_degrades_search_only (Task 1's test, the direct analog now in-file)
  - 04-RESEARCH.md "Code Examples" → "Captured Signature 2" — the verbatim stderr, the swiftpm-path coverage note, and the second-token discussion (Open Question 2 / A2)
  </read_first>
  <behavior>
  - RED: the IndexStore carrier matches nothing yet — the new test must fail with IndexingError propagating before the constant is touched.
  - GREEN: identical registry assertions to Task 1, with status_reason equal to the second Swift entry's reason verbatim.
  </behavior>
  <action>
  RED first. Add test_swift_no_index_store_signature_degrades_search_only immediately after Task 1's test, same structure and same three monkeypatches. Carrier is the exact captured stderr with only the store path rewritten to a tmp-style path: Error: Build succeeded but no IndexStore was produced at {tmp}/derived-data/Index.noindex/DataStore. This commonly happens when the code being indexed cannot compile on this host (for example, Apple-platform-only imports such as UIKit/WatchKit/WidgetKit on a non-macOS host, or a missing SDK). Provenance comment: captured 2026-08-23 from scip-swift 0.3.0; trigger shape is an .xcodeproj whose target's Sources build phase is empty (build exits 0, zero Swift compiled, argv --build-tool xcodebuild); the identical wording occurs on the swiftpm backend with a different store path, so one entry covers both backends. Run it; MUST fail. Commit: test(04-01): add failing swift no-index-store pinning test.
  GREEN. Append the fifth entry to _SEARCH_ONLY_SIGNATURES after Task 1's entry: tokens exactly ("Build succeeded but no IndexStore was produced",) — single token on purpose, the AGP single-token precedent: the sentence-long headline is already unique to this class across all nine capture shapes (04-RESEARCH.md keep-hard exhibits), and a second token drawn from the explanatory parenthetical would couple the signature to the prose most likely to be reworded while adding no false-match protection; miss-risk degrades safely to hard failure by the locked drift rule. Reason exactly: the build completed but produced no index store — scip-swift cannot extract symbols from a build that emits none (cause only, D-11). Look the reason up in the test by the exact token tuple, same tripwire idiom. Nothing else changes. Run the test; MUST pass. Commit: feat(04-01): add scip-swift no-index-store signature.
  </action>
  <verify>
  <automated>uv run pytest "tests/test_index_cli.py::test_swift_no_index_store_signature_degrades_search_only" -q</automated>
  </verify>
  <acceptance_criteria>
  - Source: _SEARCH_ONLY_SIGNATURES contains a fifth entry with token tuple exactly ("Build succeeded but no IndexStore was produced",) and the reason string quoted verbatim in <action>; the single-token choice is not second-guessed by other edits.
  - Test: test_swift_no_index_store_signature_degrades_search_only passes with all four registry-field assertions and the provenance comment naming the empty-Sources trigger and the swiftpm-backend coverage.
  - TDD gates: test(...) commit precedes feat(...) commit for this entry too.
  - Suite: uv run pytest tests/test_index_cli.py -m "not integration" -q is green at 188 passed (186 baseline + 2 new), 12 deselected.
  </acceptance_criteria>
  <done>Both captured failure classes degrade automatically with origin signature; both pinning tests embed the exact captured stderr with provenance comments; quick suite green.</done>
</task>

<task type="auto">
  <name>Task 3: SC3 negative pins — wrappers, empty carriers, first-match order</name>
  <files>tests/test_index_cli.py</files>
  <read_first>
  - src/jarvis/index_cli.py:126-132 — the _search_only_reason loop these tests exercise directly
  - src/jarvis/index_cli.py:430-436 — the _run failure carrier shape (header line, then stdout, then stderr) the empty-carrier test reconstructs
  - 04-RESEARCH.md "Keep-hard exhibits" table — the captured wrapper-plus-class-line pairs asserted below
  - tests/test_index_cli.py:1896-1947 — the local import idiom for _search_only_reason's module
  </read_first>
  <action>
  Add three pure-function tests (no tmp_path, no monkeypatch, no fixtures) directly after Task 2's test, importing _search_only_reason from jarvis.index_cli:
  1. test_swift_generic_build_failure_wrapper_never_matches — assert _search_only_reason returns None for each keep-hard exhibit behind its generic wrapper (docstring cites 04-RESEARCH.md keep-hard table, captured 2026-08-23): the wrapper Error: 'swift build' failed with exit code 1: followed in turn by error: manifest parse error; by package ... is using Swift tools version 999.0.0 but the installed version is 6.2.4; by The package does not contain a buildable target.; and by no such module 'UIKit'; plus the wrapper Error: 'xcodebuild' failed with exit code 65: followed by ** BUILD FAILED **. These are the fixable-failure shapes (repo bugs, environment, host-compile issues) that must keep failing hard — the SC3 prohibition.
  2. test_empty_or_stdout_only_failure_carriers_never_match — reconstruct the real carrier shape from _run (a step+argv header line, then stdout, then stderr) and assert None for: header-only (empty stdout and stderr); header plus whitespace-only stderr; header plus stdout-only content (e.g. a Wrote 0 document(s)-style line on a non-zero exit). Tokens must never derive from the header line, which embeds per-run cache and scratch paths — this pins that property.
  3. test_search_only_reason_first_listed_match_wins — assert that a synthetic carrier concatenating BOTH captured Swift error strings returns the no-build-system entry's reason (it precedes the IndexStore entry in the list), and that a carrier concatenating the Kotlin analog text (e: java.lang.AbstractMethodError: org.jetbrains.kotlin.fir...) with the no-build-system string returns the Kotlin reason (Kotlin entries precede Swift). Look both reasons up by exact token tuple, same tripwire idiom. This pins the first-match list-order semantics of the unchanged loop and the mutual exclusivity of the Swift pair in practice.
  Commit: test(04-01): pin wrapper negatives, empty carriers, and first-match order.
  </action>
  <verify>
  <automated>uv run pytest "tests/test_index_cli.py::test_swift_generic_build_failure_wrapper_never_matches" "tests/test_index_cli.py::test_empty_or_stdout_only_failure_carriers_never_match" "tests/test_index_cli.py::test_search_only_reason_first_listed_match_wins" -q</automated>
  </verify>
  <acceptance_criteria>
  - Tests: all three pass; the wrapper test asserts all five keep-hard lines behind their two wrappers plus the xcodebuild variant; the empty-carrier test asserts the header-only, whitespace-stderr, and stdout-only shapes; the order test asserts both the Swift-pair and Kotlin-precedence composites.
  - Prohibition coverage: the SC3 wrapper prohibition and the empty-probe and ordering-probe predicates from the phase coverage report (/tmp/phase4_coverage.json) are each mechanically pinned by a named assertion in these tests.
  - Suite: uv run pytest tests/test_index_cli.py -m "not integration" -q is green at 191 passed (186 baseline + 5 new), 12 deselected.
  </acceptance_criteria>
  <done>Unverified Swift failures are provably never degraded: generic wrappers, empty/stdout-only carriers, and multi-match ordering are all pinned by unit tests, and the quick suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo content → scip-swift stderr | untrusted build output (a hostile repo can echo arbitrary text) crosses into the signature matcher |
| matcher → registry/status surfaces | matched reason text is persisted via the existing parameterized sqlite upsert and shown in `jarvis status` / `getIndexStatus` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-4-01 | Tampering | `_SEARCH_ONLY_SIGNATURES` matching | low | accept | A hostile repo echoing a signature string forces search-only instead of hard failure, but the zoekt shards index exactly the same git-tracked content a successful run would — no privilege or data boundary crossed; identical exposure since phase 1 (Kotlin/AGP); visible via origin signature in status |
| T-4-02 | Tampering | token breadth in the two new entries | high | mitigate | Overly broad tokens (the generic wrappers) would launder every unverified Swift failure into silent degradation — the exact SC3 violation. Tokens restricted to the two class-specific path-free strings; pinned by test_swift_generic_build_failure_wrapper_never_matches (both wrappers + all five keep-hard lines → None) and test_empty_or_stdout_only_failure_carriers_never_match |
| T-4-03 | Repudiation | signature-branch registry upsert | low | mitigate | A silently degraded repo could hide its cause; existing path persists origin signature + matched reason + full stderr (phase 1), asserted end-to-end by both pinning tests' four registry-field assertions — no new code needed |

Package legitimacy gate: not applicable — no packages installed by this phase (04-RESEARCH.md audit section).
</threat_model>

<verification>
Phase-level checks (after Task 3):

1. Quick suite: `uv run pytest tests/test_index_cli.py -m "not integration" -q` → 191 passed (measured baseline 186 + 5 new), 12 deselected.
2. Full CI gate: `uv run pytest -m "not integration" -rs` → green, zero failures (baseline measured 2026-08-23 pre-plan: all unit tests passing).
3. Regression set (covered by check 2, named for the reviewer): test_signature_fallback_stamps_signature_origin_and_matched_reason (Kotlin pin unchanged), test_signature_match_preempts_the_degrade_branch_on_an_opted_in_repo (adjacency probe — signature still preempts the phase-3 degrade branch), test_check_scip_swift_version_rejects_v030_real_format / test_check_scip_swift_version_accepts_v030_real_format (pre-pipeline gate untouched).
4. Live local smoke (NOT committed — phase-2 02-03 precedent of local proof only; binary present on this machine per 04-RESEARCH.md environment table). Recreate the shapes from 04-RESEARCH.md capture recipes (or reuse /tmp/jarvis-p4-capture/repos/<shape> if still present) and run each with a scratch data dir (JARVIS_DATA_DIR=/tmp/jarvis-p4-smoke/data):
   - no-build-system shape: `uv run jarvis index <repo> --slug p4-smoke-nobuild` exits 0, stderr contains `note: p4-smoke-nobuild cannot be SCIP-indexed`, and `uv run jarvis status p4-smoke-nobuild` reports origin signature with the matched reason and recovery command.
   - empty-Sources .xcodeproj shape: same assertions with the no-IndexStore reason.
   - broken-manifest shape (keep-hard): non-zero exit, failed_hard row, nothing published — SC3 proven live.
</verification>

<success_criteria>
- SC1: both captured failure classes degrade to search-only automatically with no opt-in, status reporting origin signature and the matched reason (unit-pinned; live-smoked locally).
- SC2: both entries embed tokens captured verbatim from the pinned scip-swift 0.3.0 binary's real stderr, each pinned by a unit test embedding the exact captured output with provenance comment.
- SC3: a Swift failure matching no signature still fails hard — unit-pinned for wrappers, empty/stdout-only carriers, and first-match order; live-smoked on a keep-hard shape.
- The production diff is append-only to `_SEARCH_ONLY_SIGNATURES` (plus its comment); full unit suite green.
</success_criteria>

<output>
Create `.planning/phases/04-swift-failure-signatures/04-01-SUMMARY.md` when done. TDD mode is enabled: record the RED (failing-test) and GREEN (entry-append) runs for Tasks 1-2 and the commit list (`test(04-01): ...` / `feat(04-01): ...` / final `test(04-01): ...`), plus the live-smoke results from the verification section.
</output>
