# Demo fixture

A small Python project used to demonstrate jarvis's `findReferences` vs raw
grep. The `AuthService` symbol appears in code files and in this README's
prose, making grep results noisy.
